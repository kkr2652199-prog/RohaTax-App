# 배포 서버 스튜디오 버그 수정 - 3단계 해결

**2단계 완료**: ✅ 진단 완료
- ✅ assets 폴더에 CSS, JS 파일 존재
- ✅ 서비스가 정상 실행 중 (active (running))
- ✅ 경로 확인 완료 (`/home/ubuntu/RohaTax-App/kweon21/dist`)

---

## 🔍 발견된 문제점

### 문제 1: 빌드 파일 버전 차이
- 로컬: `index-CgcUtD30.css`, `index-fdpCDPfE.js`
- 배포 서버: `index-C_y_200_.js`, `index-D_200_.css` (다른 파일명)

**의미**: 배포 서버의 빌드 파일이 로컬과 다를 수 있습니다.

### 문제 2: 서비스가 오래된 코드 실행 중
- 서비스가 1시간 전에 시작됨 (05:19)
- 최신 빌드 파일이 반영되지 않았을 수 있음

---

## 📋 3단계: 해결 방법

### 방법 1: 서비스 재시작 (가장 간단)

배포 서버 터미널에서:

```bash
# 1. 서비스 재시작
sudo systemctl restart rohatax

# 2. 서비스 상태 확인
sudo systemctl status rohatax

# 3. 최근 로그 확인
sudo journalctl -u rohatax -n 20 --no-pager
```

**확인 사항:**
- 서비스가 `active (running)` 상태인가?
- 오류 메시지가 없는가?

---

### 방법 2: 최신 빌드 파일 확인 및 업데이트

#### 2-1. 로컬에서 최신 빌드 확인

로컬 컴퓨터에서:

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. kweon21 폴더로 이동
cd kweon21

# 3. 최신 빌드 실행
npm run build

# 4. 빌드 결과 확인
dir dist\assets
```

#### 2-2. Git에 추가 및 푸시

로컬 컴퓨터에서:

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. 빌드 파일 추가
git add kweon21/dist/

# 3. 커밋
git commit -m "Update kweon21 dist build files"

# 4. 하늘저장소에 푸시
git push origin main
```

#### 2-3. 배포 서버에서 가져오기

배포 서버 터미널에서:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. 하늘저장소에서 최신 코드 가져오기
git pull origin main

# 3. 빌드 파일 확인
ls -lh kweon21/dist/assets/

# 4. 서비스 재시작
sudo systemctl restart rohatax
```

---

## 📋 4단계: 브라우저 확인

### 브라우저에서 확인

1. **브라우저 캐시 삭제**
   - Chrome: Ctrl+Shift+Delete
   - 또는 시크릿 모드로 접속

2. **https://www.rohatax.com/studio/ 접속**

3. **확인 사항:**
   - React 앱이 iframe 안에 로드되는가?
   - API 키 설정 가이드가 정상적으로 표시되는가?
   - 콘솔에 오류가 없는가? (F12 → Console)

---

## 🔍 문제 해결 체크리스트

- [ ] 서비스 재시작 완료
- [ ] 최신 빌드 파일 확인
- [ ] 브라우저 캐시 삭제
- [ ] 페이지 정상 작동 확인

---

## ⚠️ 주의사항

1. **서비스 재시작 시**
   - 서비스가 잠시 중단될 수 있습니다 (몇 초)
   - 재시작 후 자동으로 다시 시작됩니다

2. **브라우저 캐시**
   - 오래된 파일이 캐시되어 있을 수 있습니다
   - 시크릿 모드로 테스트하는 것을 권장합니다

---

## 📝 다음 단계

**먼저 방법 1 (서비스 재시작)을 시도하세요:**

```bash
sudo systemctl restart rohatax
sudo systemctl status rohatax
```

그 다음 브라우저에서 확인하고, 여전히 문제가 있으면 방법 2를 진행하세요.

