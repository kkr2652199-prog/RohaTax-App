# 스마트폰에서 homepage1 접속 가이드

## 🚀 빠른 시작

### 방법 1: 같은 WiFi 사용 (가장 간단) ⭐ 추천

1. **PC에서 서버 시작**
   - `start_server_mobile.bat` 파일을 더블클릭하여 실행
   - 서버가 시작되면 화면에 접속 주소가 표시됩니다

2. **스마트폰에서 접속**
   - 스마트폰이 PC와 **같은 WiFi**에 연결되어 있어야 합니다
   - 스마트폰 브라우저에서 표시된 주소 입력
   - 예: `http://175.210.150.19:5001`

3. **방화벽 허용 (필요시)**
   - Windows 방화벽에서 포트 5001 허용 필요할 수 있습니다
   - 방화벽 경고가 뜨면 "액세스 허용" 클릭

---

### 방법 2: Ngrok 사용 (다른 네트워크에서도 접속 가능)

1. **Ngrok 설치** (처음 한 번만)
   - https://ngrok.com/download 접속
   - Windows 버전 다운로드
   - 압축 해제 후 `ngrok.exe`를 homepage1 폴더에 복사

2. **서버 시작**
   - `start_server_mobile.bat` 실행 (포트 5001)

3. **Ngrok 터널 시작**
   - `start_ngrok_tunnel.bat` 실행
   - 화면에 표시되는 URL 복사
   - 예: `https://xxxx-xxx-xxx-xxx.ngrok-free.app`

4. **스마트폰에서 접속**
   - 복사한 URL을 스마트폰 브라우저에 입력
   - 인터넷 어디서나 접속 가능합니다!

---

## 📱 접속 주소 확인 방법

### PC의 IP 주소 확인

**Windows PowerShell에서:**
```powershell
ipconfig | findstr IPv4
```

**또는 명령 프롬프트에서:**
```cmd
ipconfig
```
→ "IPv4 주소" 항목 확인

---

## 🔧 문제 해결

### 1. "연결할 수 없음" 오류

**원인:** 방화벽이 포트를 차단하고 있음

**해결:**
1. Windows 설정 → 개인 정보 보호 및 보안 → Windows 보안
2. 방화벽 및 네트워크 보호
3. 고급 설정
4. 인바운드 규칙 → 새 규칙
5. 포트 선택 → TCP → 특정 로컬 포트: 5001
6. 연결 허용 → 다음 → 다음 → 이름: "Flask Server 5001" → 완료

### 2. "서버를 찾을 수 없음" 오류

**확인 사항:**
- PC와 스마트폰이 같은 WiFi에 연결되어 있는지 확인
- 서버가 정상적으로 실행 중인지 확인 (`start_server_mobile.bat` 창 확인)
- IP 주소가 올바른지 확인

### 3. 포트가 이미 사용 중

**해결:**
- 다른 서버가 실행 중일 수 있습니다
- 작업 관리자에서 `python.exe` 프로세스 종료 후 다시 시작

---

## 📝 주요 페이지 테스트

스마트폰에서 다음 페이지들을 확인하세요:

1. **메인 페이지**: `http://[IP]:5001/`
2. **유료 상점**: `http://[IP]:5001/shop`
3. **3D 체험 상점**: `http://[IP]:5001/showroom`
4. **비즈니스 라운지**: `http://[IP]:5001/biz-lounge`

---

## 💡 팁

- **개발 중**: 같은 WiFi 사용 (방법 1) - 빠르고 안정적
- **외부 테스트**: Ngrok 사용 (방법 2) - 어디서나 접속 가능
- **포트 변경**: `start_server_mobile.bat` 파일에서 `FLASK_RUN_PORT=5001` 수정

---

## ⚠️ 주의사항

- 개발/테스트 목적으로만 사용하세요
- 프로덕션 환경에서는 보안 설정을 추가하세요
- Ngrok 무료 버전은 세션이 일정 시간 후 종료될 수 있습니다

