# 배포 서버 카테고리별 주제 추천 기능 테스트 가이드

**목적**: CPU 급상승 원인 파악을 위한 기능 테스트  
**테스트 계정**: tlschs21 / kkr75223996

---

## 📋 테스트 절차

### 1단계: 로그인 및 스튜디오 접속

1. **브라우저에서 접속**
   ```
   https://www.rohatax.com
   ```

2. **로그인**
   - 사용자명: `tlschs21`
   - 비밀번호: `kkr75223996`

3. **스튜디오 페이지 접속**
   ```
   https://www.rohatax.com/studio/
   ```

---

### 2단계: 브라우저 콘솔 열기

**Chrome/Edge:**
- `F12` 키 누르기
- 또는 `Ctrl + Shift + I` (Windows)
- 또는 `Cmd + Option + I` (Mac)

**콘솔 탭 선택**
- 개발자 도구가 열리면 "Console" 탭 클릭

---

### 3단계: 카테고리별 주제 추천 기능 테스트

1. **스튜디오 페이지에서 "카테고리별 주제 추천받기" 버튼 클릭**

2. **콘솔에서 확인할 사항:**
   - ✅ 정상 작동 시:
     ```
     📝 블로그 포스트 생성 시작...
     ✅ 블로그 콘텐츠 생성 완료!
     ```
   
   - ❌ 에러 발생 시:
     ```
     API 키 가져오기 실패: ...
     Error generating blog post: ...
     Failed to generate content: ...
     ```

3. **네트워크 탭 확인 (선택사항)**
   - 개발자 도구 → "Network" 탭
   - `/api/user/apikey` 요청 확인
   - `/api/studio/generate` 요청 확인
   - 요청 상태 코드 확인 (200 = 성공, 400/500 = 에러)

---

## 🔍 확인할 주요 에러 메시지

### 1. API 키 관련 에러
```
API 키 가져오기 실패: API 키가 등록되지 않았습니다
```
**의미**: 사용자 계정에 Google API 키가 등록되지 않음  
**해결**: 마이페이지에서 Google API 키 등록

### 2. 네트워크 에러
```
API 키 조회 실패: 500
Failed to fetch
```
**의미**: 서버에서 API 키를 가져오는 중 에러 발생  
**원인**: 서버 측 문제 (데이터베이스, 코드 버그 등)

### 3. 타임아웃 에러
```
주제 생성 시간 초과 (30초). 네트워크를 확인해주세요.
```
**의미**: API 호출이 30초 내에 완료되지 않음  
**원인**: 네트워크 지연, API 서버 문제, CPU 부하

### 4. 무한 루프 가능성
콘솔에 같은 에러가 반복적으로 나타나는 경우:
```
API 키 가져오기 실패: ...
API 키 가져오기 실패: ...
API 키 가져오기 실패: ...
```
**의미**: 에러 발생 시 재시도 로직이 무한 반복  
**원인**: 에러 처리 로직 문제

---

## 📊 CPU 급상승과의 연관성

### 가능한 시나리오

1. **API 키 없음 → 무한 재시도**
   - 사용자가 API 키를 등록하지 않음
   - 프론트엔드에서 계속 재시도
   - 서버에 계속 요청 → CPU 급상승

2. **서버 측 에러 → 무한 재시도**
   - 서버에서 API 키를 가져오는 중 에러 발생
   - 프론트엔드에서 계속 재시도
   - 서버에 계속 요청 → CPU 급상승

3. **타임아웃 → 재시도**
   - API 호출이 타임아웃
   - 자동 재시도 로직 실행
   - 여러 번 재시도 → CPU 급상승

---

## 🎯 테스트 결과 기록

테스트 후 다음 정보를 기록해주세요:

### 1. 콘솔 에러 메시지
```
[에러 메시지 전체 복사]
```

### 2. 네트워크 요청 상태
- `/api/user/apikey`: [상태 코드]
- `/api/studio/generate`: [상태 코드]

### 3. 기능 작동 여부
- ✅ 정상 작동
- ❌ 에러 발생 (에러 내용: ...)

### 4. CPU 사용량 (배포 서버에서)
```bash
# 테스트 중에 배포 서버 터미널에서 실행
htop
```
- CPU 사용량이 급상승하는가?
- 어떤 프로세스가 CPU를 많이 사용하는가?

---

## 🔧 예상 해결 방법

### 문제 1: API 키 없음
**해결**: 마이페이지에서 Google API 키 등록

### 문제 2: 서버 측 에러
**해결**: 
1. 서버 로그 확인
   ```bash
   sudo journalctl -u rohatax -n 100 --no-pager | grep -i error
   ```
2. 최신 코드 반영 확인
   ```bash
   cd /home/ubuntu/RohaTax-App
   git pull origin main
   sudo systemctl restart rohatax
   ```

### 문제 3: 무한 재시도
**해결**: 프론트엔드 재시도 로직 수정 필요

---

## ✅ 체크리스트

- [ ] 로그인 성공
- [ ] 스튜디오 페이지 접속 성공
- [ ] 브라우저 콘솔 열기
- [ ] "카테고리별 주제 추천받기" 버튼 클릭
- [ ] 콘솔 에러 메시지 확인
- [ ] 네트워크 요청 상태 확인
- [ ] 배포 서버 CPU 사용량 확인 (htop)
- [ ] 테스트 결과 기록

---

**테스트 후 콘솔 에러 메시지와 네트워크 요청 상태를 공유해주세요!**




