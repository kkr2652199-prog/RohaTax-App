# 배포서버 구글 API 키 최종 확인

## 진단 결과 요약

✅ **데이터베이스**: 사용자 ID 2 (tlschs21)에 API 키 있음 (39자)
❌ **환경변수**: GOOGLE_API_KEY, GEMINI_API_KEY 모두 설정 안 됨

---

## 다음 단계: 코드 버전 확인

배포서버에서 다음 명령어로 코드가 최신인지 확인:

```bash
cd /home/ubuntu/RohaTax-App

# studio_api.py 파일에서 _get_api_key 함수 확인
grep -A 5 "def _get_api_key" routes/playground_routes/studio_api.py
```

**확인할 내용:**
- `def _get_api_key(user_id: int)` ← user_id 파라미터가 있어야 함
- `sqlite3.Row` 사용하는지
- 사용자별 키를 먼저 조회하는지

---

## 예상되는 정상 코드

```python
def _get_api_key(user_id: int) -> str:
    """
    사용자별 API 키 가져오기 (BYOK 모델)
    1. 사용자 데이터베이스에서 google_api_key 조회
    2. 없으면 서버 환경변수에서 fallback
    """
    import sqlite3
    from core.db import get_conn
    
    # 1. 사용자별 API 키 조회
    if user_id:
        try:
            with get_conn() as conn:
                conn.row_factory = sqlite3.Row
                user = conn.execute(
                    "SELECT google_api_key FROM users WHERE id = ? AND COALESCE(is_deleted, 0) = 0",
                    (user_id,)
                ).fetchone()
                
                if user and user['google_api_key']:
                    api_key = user['google_api_key'].strip()
                    if api_key and (api_key.startswith('AIzaSy') or len(api_key) >= 20):
                        return api_key
        except Exception as e:
            logger.warning(f"사용자 API 키 조회 실패: {e}")
    
    # 2. Fallback: 서버 환경변수
    api_key = os.environ.get('GOOGLE_API_KEY') or os.environ.get('GEMINI_API_KEY')
    if not api_key:
        raise ValueError("API 키가 설정되지 않았습니다...")
    return api_key
```

---

## 코드가 최신이 아닌 경우

만약 `def _get_api_key():` (user_id 없음)로 되어 있다면:

```bash
# 최신 코드 가져오기
git pull origin main

# Flask 서비스 재시작
sudo systemctl restart rohatax

# 서비스 상태 확인
sudo systemctl status rohatax
```

---

## 코드가 최신인 경우

코드가 최신인데도 작동하지 않는다면:

1. **로그 확인:**
```bash
sudo journalctl -u rohatax -n 50 --no-pager | grep -i "_get_api_key\|api_key"
```

2. **실제 API 호출 테스트:**
   - 브라우저에서 `https://www.rohatax.com/studio/` 접속
   - 로그인 후 "카테고리별 주제 추천받기" 클릭
   - 콘솔(F12)에서 에러 확인

---

## 다음 단계

1. 위 명령어로 코드 버전 확인
2. 결과 공유
3. 필요시 최신 코드 적용 및 재시작


