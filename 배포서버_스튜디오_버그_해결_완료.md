# 배포 서버 스튜디오 버그 해결 완료

**문제**: iframe이 로드되지 않음  
**원인**: `X-Frame-Options: DENY` 헤더로 인한 차단  
**해결**: `/studio/app` 라우트에 `X-Frame-Options: SAMEORIGIN` 명시적 설정

---

## 🔍 발견된 문제

### 브라우저 콘솔 오류
```
Refused to display 'https://www.rohatax.com/' in a frame because it set 'X-Frame-Options' to 'deny'.
```

### 원인
- 전역 보안 설정(`core/security_enhancement.py`)에서 `X-Frame-Options: DENY` 설정
- `/studio/app` 라우트가 iframe에서 로드되어야 하는데 차단됨

---

## ✅ 해결 방법

### 수정 내용
`routes/playground_routes/kweon21_routes.py` 파일 수정:

1. **`kweon21_app` 함수** (라인 82-86):
   - `X-Frame-Options: SAMEORIGIN` 헤더 추가

2. **`kweon21_app_assets` 함수** (라인 100-105):
   - `X-Frame-Options: SAMEORIGIN` 헤더 추가

### 수정된 코드
```python
# /studio/app 라우트
response.headers['X-Frame-Options'] = 'SAMEORIGIN'

# /studio/app/assets 라우트
resp.headers['X-Frame-Options'] = 'SAMEORIGIN'
```

---

## 📋 배포 서버에서 적용 방법

### 1단계: 하늘저장소에서 최신 코드 가져오기

배포 서버 터미널에서:

```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
```

### 2단계: 서비스 재시작

```bash
sudo systemctl restart rohatax
```

### 3단계: 서비스 상태 확인

```bash
sudo systemctl status rohatax
```

### 4단계: 브라우저에서 확인

1. **브라우저 캐시 삭제** (중요!)
   - Chrome: `Ctrl + Shift + Delete`
   - 또는 시크릿 모드로 접속

2. **https://www.rohatax.com/studio/ 접속**

3. **확인 사항:**
   - React 앱이 iframe 안에 정상적으로 로드되는가?
   - 콘솔에 오류가 없는가? (F12 → Console)

---

## ✅ 완료 체크리스트

- [x] 코드 수정 완료
- [x] 하늘저장소에 푸시 완료
- [ ] 배포 서버에서 `git pull` 실행
- [ ] 배포 서버에서 서비스 재시작
- [ ] 브라우저에서 정상 작동 확인

---

## 🎯 다음 단계

1. **배포 서버에서 위 명령어 실행**
2. **브라우저에서 확인**
3. **문제가 있으면 알려주세요**

---

**수정 완료! 배포 서버에 적용하세요!**

