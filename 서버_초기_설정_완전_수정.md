# 🔧 서버 초기 설정 완전 수정 가이드

## 📋 현재 발견된 문제들

1. ❌ SECRET_KEY 미설정 (세션 작동 안 함)
2. ❌ 데이터베이스 파일 권한 문제 (root 소유)
3. ❌ 일부 정적 파일 누락 (404 오류)
4. ❌ 서버 상태 불안정

---

## 🎯 단계별 해결 방법

### 1단계: SECRET_KEY 설정 (필수!)

**Lightsail 터미널에서 실행:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
```

<--- 복사하세요
```
python3 -c "import secrets; key = secrets.token_hex(32); print('SECRET_KEY=' + key); print('\n이 키를 .env 파일에 추가하세요!')"
```

**출력된 SECRET_KEY를 복사한 후:**

<--- 복사하세요
```
nano .env
```

**.env 파일에 다음 추가 (없으면 파일 생성):**
```
SECRET_KEY=복사한-키-여기에-붙여넣기
ENVIRONMENT=production
DEBUG=False
```

**저장:** `Ctrl + X`, `Y`, `Enter`

---

### 2단계: 데이터베이스 권한 수정

**데이터베이스 파일이 root 소유이면 ubuntu 사용자가 쓸 수 없습니다.**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
sudo chown -R ubuntu:ubuntu database/
```

<--- 복사하세요
```
chmod 644 database/*.db*
```

<--- 복사하세요
```
ls -la database/ | grep app.db
```

**확인:** `app.db` 파일이 `ubuntu ubuntu` 소유여야 합니다.

---

### 3단계: 누락된 정적 파일 확인

**404 오류가 나는 파일들:**
- `video-poster.jpg`
- `roha_conversion_demo.mp4.mp4`

**로컬에서 서버로 업로드 필요:**

**로컬 PowerShell에서 실행:**
```powershell
cd C:\Users\user\Desktop\RohaTax\homepage1
scp static/videos/roha_conversion_demo.mp4.mp4 ubuntu@52.78.116.159:/home/ubuntu/RohaTax-App/static/videos/
```

**또는 서버에서 확인:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
find static -name "*.mp4" -o -name "*poster*"
```

---

### 4단계: 서버 상태 확인 및 재시작

**기존 프로세스 종료:**

<--- 복사하세요
```
pkill -f "python3 app.py"
```

<--- 복사하세요
```
lsof -ti:5000 | xargs kill -9 2>/dev/null || true
```

<--- 복사하세요
```
lsof -ti:80 | xargs kill -9 2>/dev/null || true
```

**서버 재시작 (포트 5000 사용):**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 &
```

<--- 복사하세요
```
sleep 5 && tail -30 app.log
```

**확인 사항:**
- `SECRET_KEY` 경고가 사라졌는지
- 서버가 정상 시작되었는지 (`Running on http://0.0.0.0:5000`)

---

### 5단계: 테스트 사용자 계정 생성

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 << 'EOF'
import sqlite3
from core.password_utils import hash_password

conn = sqlite3.connect('database/app.db')
cursor = conn.cursor()

username = 'test'
password = 'test1234'
hashed_password = hash_password(password)

try:
    cursor.execute('''
        INSERT INTO users (username, password, email, is_active, approval_status, plan_type)
        VALUES (?, ?, ?, 1, 'approved', 'free')
    ''', (username, hashed_password, f'{username}@test.com'))
    conn.commit()
    print(f'✅ 테스트 사용자 생성 완료')
    print(f'   사용자명: {username}')
    print(f'   비밀번호: {password}')
except sqlite3.IntegrityError:
    print('⚠️ 사용자 이미 존재합니다.')
    # 기존 사용자 비밀번호 업데이트
    cursor.execute('UPDATE users SET password = ? WHERE username = ?', (hashed_password, username))
    conn.commit()
    print(f'✅ 비밀번호 업데이트 완료: {username} / {password}')
finally:
    conn.close()
EOF
```

---

### 6단계: 최종 확인

**서버 상태 확인:**

<--- 복사하세요
```
ps aux | grep "python3 app.py" | grep -v grep
```

<--- 복사하세요
```
lsof -i :5000
```

**로그 확인:**

<--- 복사하세요
```
tail -20 app.log | grep -i "secret\|error\|warning"
```

**예상 결과:**
- SECRET_KEY 경고가 없어야 함
- ERROR가 없어야 함

---

## 🎯 빠른 수정 (한 번에 실행)

**모든 문제를 한 번에 해결:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App && \
sudo chown -R ubuntu:ubuntu database/ && \
chmod 644 database/*.db* && \
python3 -c "import secrets; key = secrets.token_hex(32); print('SECRET_KEY=' + key)" > /tmp/secret_key.txt && \
echo "" >> .env && \
cat /tmp/secret_key.txt >> .env && \
echo "ENVIRONMENT=production" >> .env && \
echo "DEBUG=False" >> .env && \
pkill -f "python3 app.py" && \
sleep 2 && \
source venv/bin/activate && \
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 & \
sleep 5 && \
tail -30 app.log
```

---

## ✅ 완료 후 확인

1. **브라우저에서 `https://rohatax.com:5000` 접속**
2. **로그인 페이지에서 테스트:**
   - 사용자명: `test`
   - 비밀번호: `test1234`
3. **로그인 성공 시 대시보드로 이동해야 함**

---

## 🆘 여전히 문제가 있으면

**서버 로그 실시간 확인:**

<--- 복사하세요
```
tail -f app.log
```

브라우저에서 로그인 시도하면서 오류 확인

