# 🔧 로컬 vs 서버 차이점 하나씩 수정 가이드

## 📋 목표
로컬에서는 정상 작동하는데 서버에서만 문제가 있는 부분을 하나씩 찾아서 수정

---

## 1단계: 현재 상태 확인

### 서버 상태 확인

<--- 복사하세요
```
ps aux | grep "python3 app.py" | grep -v grep
```

<--- 복사하세요
```
lsof -i :80
```

<--- 복사하세요
```
tail -20 app.log | grep -i "error\|warning"
```

---

## 2단계: 브라우저에서 확인할 사항

**브라우저에서 `https://rohatax.com/` 접속 후:**

1. **페이지가 정상적으로 표시되는가?**
   - ✅ 정상 표시
   - ❌ 페이지가 깨져 있음
   - ❌ 일부 이미지/파일이 안 보임

2. **로그인 기능**
   - ✅ 로그인 페이지 접속 가능
   - ❌ 로그인 페이지 접속 불가
   - ❌ 로그인 시도 시 오류

3. **기타 기능**
   - ✅ 모든 기능 정상
   - ❌ 특정 기능만 작동 안 함

---

## 3단계: 문제별 해결 방법

### 문제 1: 페이지가 깨져 있음

**원인:** 정적 파일 경로 문제 또는 파일 누락

**확인:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
ls -la static/
```

<--- 복사하세요
```
find static -name "*.css" -o -name "*.js" | head -10
```

---

### 문제 2: 로그인 안 됨

**원인:** SECRET_KEY 또는 데이터베이스 문제

**확인:**

<--- 복사하세요
```
python3 -c "from config.settings import settings; print('SECRET_KEY:', '설정됨' if settings.SECRET_KEY else '미설정')"
```

<--- 복사하세요
```
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM users'); print('사용자 수:', cursor.fetchone()[0]); conn.close()"
```

---

### 문제 3: 특정 파일 404 오류

**확인:**

<--- 복사하세요
```
tail -50 app.log | grep "404"
```

**누락된 파일 확인 후 로컬에서 업로드 필요**

---

## 다음 단계

**먼저 브라우저에서 어떤 문제가 있는지 알려주세요:**
1. 페이지가 정상적으로 보이나요?
2. 로그인 페이지는 접속되나요?
3. 어떤 기능이 작동하지 않나요?

문제를 알려주시면 하나씩 해결하겠습니다.

