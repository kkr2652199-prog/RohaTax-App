# 🔍 배포 서버 상태 진단 명령어 모음

> **목적**: 배포 서버의 문제를 파악하기 위한 진단 명령어들  
> **주의**: 진단만 수행하며, 코드 수정이나 Git 작업은 하지 않습니다.

---

## 📋 1단계: 기본 상태 확인

### 현재 디렉토리 및 프로세스 확인
```bash
# 현재 위치 확인
pwd

# 포트 5000 사용 중인 프로세스
lsof -i :5000

# Python 프로세스 확인
ps aux | grep "python3.*app.py" | grep -v grep

# Screen 세션 확인
screen -ls
```

### Git 상태 확인 (코드 동기화 여부)
```bash
cd /home/ubuntu/RohaTax-App

# 현재 브랜치 및 상태
git status

# 최근 커밋 5개 확인
git log --oneline -5

# 원격 저장소 정보
git remote -v

# 원격과 로컬 차이 확인 (fetch 없이)
git fetch --dry-run 2>&1 || echo "fetch 확인 필요"
```

---

## 📋 2단계: 로그 파일 분석

### 최근 로그 확인
```bash
cd /home/ubuntu/RohaTax-App

# 최근 100줄 확인
tail -100 /home/ubuntu/flask_server.log

# 오류만 필터링
tail -200 /home/ubuntu/flask_server.log | grep -i "error\|exception\|failed\|traceback" -A 5

# 경고 메시지 확인
tail -200 /home/ubuntu/flask_server.log | grep -i "warning" -A 3

# Pydantic 관련 메시지
tail -200 /home/ubuntu/flask_server.log | grep -i "pydantic\|orm_mode\|from_attributes"
```

### Screen 세션 로그 확인 (실시간)
```bash
# Screen 세션 접속
screen -r rohatax

# 나가기: Ctrl+A, D
```

---

## 📋 3단계: 환경 및 설정 확인

### 환경 변수 확인
```bash
cd /home/ubuntu/RohaTax-App

echo "=== 환경 변수 ==="
echo "FLASK_RUN_HOST: ${FLASK_RUN_HOST:-미설정}"
echo "FLASK_RUN_PORT: ${FLASK_RUN_PORT:-미설정}"
echo "FLASK_ENV: ${FLASK_ENV:-미설정}"
echo "PYTHONPATH: ${PYTHONPATH:-미설정}"
```

### .env 파일 확인 (민감 정보 제외)
```bash
cd /home/ubuntu/RohaTax-App

if [ -f ".env" ]; then
    echo "=== .env 파일 존재 ==="
    echo "주요 키 확인 (값은 숨김):"
    grep -E "^[A-Z_]+=" .env | sed 's/=.*/=***/' || echo "환경 변수 없음"
else
    echo "❌ .env 파일 없음"
fi
```

### 가상환경 확인
```bash
cd /home/ubuntu/RohaTax-App

echo "=== 가상환경 상태 ==="
if [ -d "venv" ]; then
    echo "✅ venv 디렉토리 존재"
    if [ -f "venv/bin/activate" ]; then
        echo "✅ venv/bin/activate 존재"
        source venv/bin/activate
        echo "Python 버전: $(python3 --version)"
        echo "Pip 버전: $(pip --version)"
    else
        echo "❌ venv/bin/activate 없음"
    fi
else
    echo "❌ venv 디렉토리 없음"
fi
```

---

## 📋 4단계: 의존성 및 패키지 확인

### 주요 패키지 버전 확인
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate

echo "=== 주요 패키지 버전 ==="
pip list | grep -E "(Flask|Pydantic|Werkzeug|SQLAlchemy|python-dotenv)" || echo "패키지 확인 실패"
```

### requirements.txt 비교
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate

if [ -f "requirements.txt" ]; then
    echo "=== requirements.txt 존재 ==="
    echo "파일 크기: $(wc -l < requirements.txt) 줄"
    echo "주요 패키지:"
    grep -E "^(Flask|Pydantic|Werkzeug)" requirements.txt || echo "주요 패키지 없음"
else
    echo "❌ requirements.txt 없음"
fi
```

---

## 📋 5단계: 코드 및 파일 확인

### app.py 문법 검사
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate

echo "=== app.py 문법 검사 ==="
python3 -m py_compile app.py 2>&1
```

### Pydantic orm_mode 사용 여부 확인
```bash
cd /home/ubuntu/RohaTax-App

echo "=== orm_mode 사용 위치 검색 ==="
grep -r "orm_mode" --include="*.py" . 2>/dev/null | head -10 || echo "orm_mode 사용 없음"
```

### 핵심 파일 존재 확인
```bash
cd /home/ubuntu/RohaTax-App

echo "=== 핵심 파일 확인 ==="
files=("app.py" "config/settings.py" "core/db.py" ".env" "requirements.txt")
for file in "${files[@]}"; do
    if [ -f "$file" ]; then
        echo "✅ $file 존재"
    else
        echo "❌ $file 없음"
    fi
done
```

---

## 📋 6단계: 데이터베이스 확인

### 데이터베이스 파일 확인
```bash
cd /home/ubuntu/RohaTax-App

echo "=== 데이터베이스 상태 ==="
if [ -f "database/app.db" ]; then
    echo "✅ database/app.db 존재"
    ls -lh database/app.db
    echo "파일 권한:"
    ls -l database/app.db
else
    echo "❌ database/app.db 없음"
fi

# database 디렉토리 확인
if [ -d "database" ]; then
    echo "database 디렉토리 내용:"
    ls -la database/ | head -10
else
    echo "❌ database 디렉토리 없음"
fi
```

### 데이터베이스 접근 테스트
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate

echo "=== 데이터베이스 접근 테스트 ==="
python3 -c "
from core.db import get_conn
try:
    with get_conn() as conn:
        result = conn.execute('SELECT 1').fetchone()
        print('✅ 데이터베이스 접근 성공')
except Exception as e:
    print(f'❌ 데이터베이스 접근 실패: {e}')
" 2>&1
```

---

## 📋 7단계: 서버 시작 시도 (테스트용)

### 수동 실행 테스트 (백그라운드 아님)
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate

export FLASK_RUN_HOST=0.0.0.0
export FLASK_RUN_PORT=5000
export FLASK_ENV=production

echo "=== 서버 시작 테스트 (5초 후 종료) ==="
timeout 5 python3 app.py 2>&1 | head -50 || echo "서버 시작 실패 또는 타임아웃"
```

---

## 📋 8단계: 종합 진단 스크립트

### 한 번에 실행할 진단 스크립트
```bash
cd /home/ubuntu/RohaTax-App

echo "=========================================="
echo "🔍 배포 서버 종합 진단"
echo "=========================================="
echo ""

# 1. 프로세스 상태
echo "1️⃣ 프로세스 상태"
echo "----------------------------------------"
lsof -i :5000 || echo "포트 5000 사용 중인 프로세스 없음"
ps aux | grep "python3.*app.py" | grep -v grep || echo "실행 중인 Flask 프로세스 없음"
screen -ls || echo "Screen 세션 없음"
echo ""

# 2. Git 상태
echo "2️⃣ Git 상태"
echo "----------------------------------------"
git status --short
echo "최근 커밋:"
git log --oneline -3
echo ""

# 3. 로그 확인
echo "3️⃣ 최근 로그 (마지막 30줄)"
echo "----------------------------------------"
if [ -f /home/ubuntu/flask_server.log ]; then
    tail -30 /home/ubuntu/flask_server.log
else
    echo "⚠️ flask_server.log 파일 없음"
fi
echo ""

# 4. 환경 변수
echo "4️⃣ 환경 변수"
echo "----------------------------------------"
echo "FLASK_RUN_HOST: ${FLASK_RUN_HOST:-미설정}"
echo "FLASK_RUN_PORT: ${FLASK_RUN_PORT:-미설정}"
echo "FLASK_ENV: ${FLASK_ENV:-미설정}"
echo ""

# 5. 가상환경
echo "5️⃣ 가상환경"
echo "----------------------------------------"
if [ -d "venv" ] && [ -f "venv/bin/activate" ]; then
    source venv/bin/activate
    python3 --version
    pip --version | head -1
else
    echo "❌ 가상환경 문제"
fi
echo ""

# 6. 핵심 파일
echo "6️⃣ 핵심 파일 확인"
echo "----------------------------------------"
[ -f "app.py" ] && echo "✅ app.py" || echo "❌ app.py 없음"
[ -f ".env" ] && echo "✅ .env" || echo "❌ .env 없음"
[ -f "requirements.txt" ] && echo "✅ requirements.txt" || echo "❌ requirements.txt 없음"
[ -f "database/app.db" ] && echo "✅ database/app.db" || echo "❌ database/app.db 없음"
echo ""

# 7. Pydantic 경고 확인
echo "7️⃣ Pydantic 경고 확인"
echo "----------------------------------------"
grep -r "orm_mode" --include="*.py" . 2>/dev/null | head -3 || echo "orm_mode 사용 없음"
echo ""

echo "=========================================="
echo "✅ 진단 완료"
echo "=========================================="
```

---

## 📊 진단 결과 해석 가이드

### 정상 상태
- ✅ 포트 5000에서 프로세스 실행 중
- ✅ Screen 세션 `rohatax` 활성
- ✅ 로그에 오류 없음
- ✅ Git 상태 clean
- ✅ 모든 핵심 파일 존재

### 문제 가능성
- ❌ 포트 5000 사용 중인 프로세스 없음 → 서버가 시작되지 않음
- ❌ 로그에 "FAILED" 또는 "Error" 메시지 → 오류 원인 확인 필요
- ❌ Git 상태에 변경사항 있음 → 코드 동기화 문제
- ❌ orm_mode 사용 발견 → Pydantic V2 호환성 문제
- ❌ .env 파일 없음 → 환경 변수 누락
- ❌ database/app.db 없음 → 데이터베이스 초기화 필요

---

## 🎯 다음 단계

진단 결과를 확인한 후:
1. **로그 파일의 오류 메시지** 확인
2. **Git 상태** 확인 (코드 동기화 여부)
3. **환경 변수** 확인 (.env 파일)
4. **의존성 버전** 확인 (requirements.txt)

진단 결과를 공유해주시면 구체적인 해결 방안을 제시하겠습니다.

