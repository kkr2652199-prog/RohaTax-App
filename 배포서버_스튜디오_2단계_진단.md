# 배포 서버 스튜디오 버그 수정 - 2단계 진단

**1단계 완료**: ✅ 파일 존재 확인 완료
- ✅ `kweon21/dist/` 폴더 존재
- ✅ `kweon21/dist/index.html` 파일 존재 (2.9K)
- ✅ `kweon21/dist/assets/` 폴더 존재

---

## 📋 2단계: 상세 진단

### 명령어 1: assets 폴더 내용 확인

배포 서버 터미널에서:

```bash
ls -lh kweon21/dist/assets/
```

**확인 사항:**
- CSS 파일이 있는가? (예: `index-*.css`)
- JS 파일이 있는가? (예: `index-*.js`)
- 파일 크기가 0이 아닌가?

---

### 명령어 2: 서비스 상태 확인

```bash
sudo systemctl status rohatax
```

**확인 사항:**
- 서비스가 `active (running)` 상태인가?
- 오류 메시지가 있는가?

---

### 명령어 3: 서비스 로그 확인

```bash
sudo journalctl -u rohatax -n 50 --no-pager
```

**확인 사항:**
- `/studio` 경로 관련 오류가 있는가?
- `kweon21` 관련 오류가 있는가?

---

### 명령어 4: 실제 파일 경로 확인

```bash
# Python이 찾는 경로 확인
python3 -c "import os; print(os.path.abspath('routes/playground_routes/../../kweon21/dist'))"
```

---

## 🔍 예상 문제점

### 문제 1: assets 파일이 없는 경우
**증상**: `ls -lh kweon21/dist/assets/` 결과가 비어있음

**해결**: 로컬에서 다시 빌드하고 업로드

### 문제 2: 파일 경로 문제
**증상**: Python이 다른 경로를 찾고 있음

**해결**: 경로 확인 및 수정

### 문제 3: 서비스가 오래된 코드를 실행 중
**증상**: 서비스가 재시작되지 않아서 오래된 코드 실행

**해결**: 서비스 재시작

---

## 📋 다음 단계

위 명령어들을 실행하고 결과를 알려주세요!

특히:
1. `ls -lh kweon21/dist/assets/` 결과
2. `sudo systemctl status rohatax` 결과
3. 브라우저에서 https://www.rohatax.com/studio/ 접속 시 어떤 화면이 보이는지

