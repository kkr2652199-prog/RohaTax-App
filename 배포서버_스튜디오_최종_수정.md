# 배포 서버 스튜디오 최종 수정

**문제**: 전역 보안 미들웨어가 `/studio/app`의 `X-Frame-Options: SAMEORIGIN`을 덮어씀  
**해결**: 전역 보안 미들웨어에서 `/studio/app` 경로 예외 처리

---

## 🔍 발견된 문제

### 브라우저 콘솔 오류 (여전히 발생)
```
Refused to display 'https://www.rohatax.com/' in a frame because it set 'X-Frame-Options' to 'deny'.
```

### 원인
- `/studio/app` 라우트에서 `X-Frame-Options: SAMEORIGIN` 설정
- 하지만 전역 보안 미들웨어(`core/security_enhancement.py`)가 모든 응답에 `X-Frame-Options: DENY` 설정
- 전역 미들웨어가 나중에 실행되어 `/studio/app`의 설정을 덮어씀

---

## ✅ 해결 방법

### 수정 내용
`core/security_enhancement.py` 파일의 `_setup_security` 함수 수정:

**수정 전:**
```python
@self.app.after_request
def set_security_headers(response):
    headers = self.security_config.get_security_headers()
    for header, value in headers.items():
        response.headers[header] = value
    return response
```

**수정 후:**
```python
@self.app.after_request
def set_security_headers(response):
    # /studio/app 경로는 iframe에서 로드되므로 X-Frame-Options 예외 처리
    from flask import request
    if request.path.startswith('/studio/app'):
        # X-Frame-Options만 SAMEORIGIN으로 설정, 나머지는 전역 설정 유지
        headers = self.security_config.get_security_headers()
        for header, value in headers.items():
            if header == 'X-Frame-Options':
                response.headers[header] = 'SAMEORIGIN'
            else:
                response.headers[header] = value
    else:
        # 다른 경로는 전역 설정 그대로 적용
        headers = self.security_config.get_security_headers()
        for header, value in headers.items():
            response.headers[header] = value
    return response
```

---

## 📋 배포 서버에서 적용 방법

### 1단계: 하늘저장소에서 최신 코드 가져오기

배포 서버 터미널에서:

```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
```

### 2단계: 서비스 재시작

```bash
sudo systemctl restart rohatax
```

### 3단계: 서비스 상태 확인

```bash
sudo systemctl status rohatax
```

### 4단계: 브라우저에서 확인

1. **브라우저 캐시 삭제** (매우 중요!)
   - Chrome: `Ctrl + Shift + Delete`
   - 또는 시크릿 모드로 접속

2. **https://www.rohatax.com/studio/ 접속**

3. **확인 사항:**
   - React 앱이 iframe 안에 정상적으로 로드되는가?
   - 콘솔에 X-Frame-Options 오류가 없는가? (F12 → Console)

---

## ✅ 완료 체크리스트

- [x] 코드 수정 완료 (전역 보안 미들웨어)
- [x] 하늘저장소에 푸시 완료
- [ ] 배포 서버에서 `git pull` 실행
- [ ] 배포 서버에서 서비스 재시작
- [ ] 브라우저에서 정상 작동 확인

---

## 🎯 다음 단계

1. **배포 서버에서 위 명령어 실행**
2. **브라우저 캐시 삭제 후 확인**
3. **문제가 있으면 알려주세요**

---

**이번 수정으로 전역 보안 미들웨어도 예외 처리되어 완전히 해결됩니다!**

