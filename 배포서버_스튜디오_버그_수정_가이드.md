# 배포 서버 스튜디오 버그 수정 가이드 (초보자용)

**문제**: 배포 서버의 `/studio` 페이지가 로컬처럼 작동하지 않음

---

## 📋 1단계: 현재 상태 확인

### 로컬 상태 (확인 완료 ✅)
- ✅ `kweon21/dist/` 폴더 존재
- ✅ `index.html` 파일 존재
- ✅ `assets/` 폴더에 CSS, JS 파일 존재

### 배포 서버 확인 필요
배포 서버 터미널에서 다음 명령어를 실행하세요:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. kweon21 폴더 확인
ls -la kweon21/

# 3. dist 폴더 확인
ls -la kweon21/dist/

# 4. index.html 파일 확인
ls -lh kweon21/dist/index.html
```

---

## 📋 2단계: 문제 진단

### 시나리오 A: dist 폴더가 없는 경우
**증상**: `ls: cannot access 'kweon21/dist/': No such file or directory`

**해결**: 빌드 파일을 배포 서버에 업로드해야 합니다.

### 시나리오 B: dist 폴더는 있지만 파일이 없는 경우
**증상**: `dist/` 폴더는 있지만 `index.html`이 없음

**해결**: 빌드 파일을 다시 업로드해야 합니다.

### 시나리오 C: dist 폴더와 파일이 모두 있는 경우
**증상**: 파일은 있지만 페이지가 작동하지 않음

**해결**: 경로 문제이거나 빌드 파일이 오래된 것일 수 있습니다.

---

## 📋 3단계: 해결 방법

### 방법 1: 로컬에서 빌드 후 Git으로 업로드 (추천)

#### 3-1. 로컬에서 빌드 확인

로컬 컴퓨터에서:

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. kweon21 폴더로 이동
cd kweon21

# 3. 빌드 실행 (이미 빌드되어 있으면 생략 가능)
npm run build

# 4. 빌드 결과 확인
dir dist
```

#### 3-2. Git에 추가 및 푸시

로컬 컴퓨터에서:

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. 빌드 파일 추가
git add kweon21/dist/

# 3. 커밋
git commit -m "Add kweon21 dist build files for deployment"

# 4. 하늘저장소에 푸시
git push origin main
```

#### 3-3. 배포 서버에서 가져오기

배포 서버 터미널에서:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. 하늘저장소에서 최신 코드 가져오기
git pull origin main

# 3. dist 폴더 확인
ls -la kweon21/dist/
```

---

### 방법 2: 배포 서버에서 직접 빌드

배포 서버 터미널에서:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. Node.js 설치 확인
node --version
npm --version

# Node.js가 없으면 설치 필요
# sudo apt update
# sudo apt install nodejs npm

# 3. kweon21 폴더로 이동
cd kweon21

# 4. 의존성 설치
npm install

# 5. 빌드 실행
npm run build

# 6. 빌드 결과 확인
ls -la dist/
```

---

## 📋 4단계: 확인 및 테스트

### 배포 서버에서 확인

```bash
# 1. 파일 확인
ls -lh /home/ubuntu/RohaTax-App/kweon21/dist/index.html
ls -la /home/ubuntu/RohaTax-App/kweon21/dist/assets/

# 2. 서비스 재시작 (필요한 경우)
sudo systemctl restart rohatax

# 3. 서비스 상태 확인
sudo systemctl status rohatax
```

### 브라우저에서 확인

1. https://www.rohatax.com/studio/ 접속
2. React 앱이 iframe 안에 정상적으로 로드되는지 확인
3. API 키 설정 가이드가 정상적으로 표시되는지 확인

---

## 🔍 문제 해결 체크리스트

- [ ] 배포 서버에 `kweon21/dist/` 폴더가 있는가?
- [ ] `kweon21/dist/index.html` 파일이 있는가?
- [ ] `kweon21/dist/assets/` 폴더에 CSS, JS 파일이 있는가?
- [ ] 파일 권한이 올바른가? (ubuntu:ubuntu)
- [ ] 서비스가 정상적으로 실행 중인가?

---

## ⚠️ 주의사항

1. **빌드 파일은 Git에 포함되어야 합니다**
   - `.gitignore`에 `kweon21/dist/`가 있으면 제거해야 합니다

2. **빌드 후에는 항상 테스트**
   - 로컬에서 먼저 테스트
   - 배포 서버에 업로드 후 다시 테스트

3. **권한 확인**
   - 파일 소유자: `ubuntu:ubuntu`
   - 읽기 권한: 모든 사용자

---

## 📝 다음 단계

1. **1단계 실행**: 배포 서버에서 현재 상태 확인
2. **결과 보고**: 어떤 시나리오인지 알려주세요
3. **해결 방법 선택**: 방법 1 또는 방법 2 선택
4. **실행 및 확인**: 해결 방법 실행 후 테스트

---

**먼저 1단계를 실행하고 결과를 알려주세요!**

