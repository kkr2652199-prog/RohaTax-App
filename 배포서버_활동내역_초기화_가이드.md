# 배포 서버 유저 활동 내역 초기화 가이드

**목적**: 유저 활동 내역(activity_logs)만 초기화  
**보존**: 유저 정보, 시스템 로직은 절대 건들지 않음

---

## 🎯 초기화 대상

### ✅ 삭제할 것
- **유저 활동 내역** (activity_logs 테이블)
  - 로그인/로그아웃 기록
  - 파일 변환 기록
  - 토큰 사용 기록
  - 관리자 활동 기록
  - 모든 활동 로그

### ❌ 절대 건들지 않을 것
- **유저 정보** (users 테이블)
  - 모든 사용자 계정
  - 이메일, 등급, 토큰 잔액 등
- **시스템 로직**
  - 모든 기능 그대로 유지
- **다른 테이블**
  - payment_history, token_history 등은 그대로 유지

---

## 📋 실행 방법

### 배포 서버 터미널에서

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App
```

```bash
# 2. 하늘저장소에서 최신 스크립트 가져오기
git pull origin main
```

```bash
# 3. 스크립트 실행 권한 부여
chmod +x scripts/clear_activity_logs_only.py
```

```bash
# 4. 스크립트 실행
python3 scripts/clear_activity_logs_only.py
```

---

## ⚠️ 안전장치

### 자동 백업
- 스크립트 실행 전 **자동으로 데이터베이스 백업** 생성
- 백업 위치: `database/backups/app_db_before_clear_activity_YYYYMMDD_HHMMSS.db`

### 확인 절차
- 실행 전 확인 메시지 표시
- 삭제 전/후 레코드 수 표시
- 유저 정보 확인

---

## 📊 실행 결과 예시

```
============================================================
유저 활동 내역 초기화 스크립트
============================================================

⚠️  주의사항:
  - 유저 정보는 절대 건들지 않습니다
  - 시스템 로직은 그대로 유지됩니다
  - activity_logs 테이블만 삭제됩니다

유저 활동 내역을 초기화하시겠습니까? (yes/no): yes

📦 데이터베이스 백업 생성 중...
✅ 백업 완료

============================================================
유저 활동 내역 초기화
============================================================

[삭제 전] activity_logs: 1,234개

🗑️  활동 로그 삭제 중...
[삭제 후] activity_logs: 0개

✅ 1,234개 활동 로그 삭제 완료

============================================================
✅ 유저 정보 확인 (절대 건들지 않음)
============================================================
총 유저 수: 10명

[샘플 유저 정보]
  ID: 1, 사용자명: user1, 이메일: user1@example.com, 등급: vip, 토큰: 100
  ID: 2, 사용자명: user2, 이메일: user2@example.com, 등급: gold, 토큰: 50
  ...

============================================================
✅ 작업 완료!
============================================================
백업 파일: database/backups/app_db_before_clear_activity_20260101_132800.db
삭제된 활동 로그: 1,234개

✅ 유저 정보는 그대로 유지되었습니다.
✅ 시스템 로직은 그대로 유지되었습니다.
```

---

## ✅ 완료 확인

### 활동 로그 확인
```bash
# 배포 서버에서
sqlite3 database/app.db "SELECT COUNT(*) FROM activity_logs;"
# 결과: 0 (모두 삭제됨)
```

### 유저 정보 확인
```bash
# 유저 수 확인 (변화 없어야 함)
sqlite3 database/app.db "SELECT COUNT(*) FROM users;"

# 유저 정보 확인
sqlite3 database/app.db "SELECT id, username, email, plan_type, token_balance FROM users LIMIT 5;"
```

---

## 🆘 문제 발생 시

### 백업에서 복원
```bash
# 백업 파일 확인
ls -lh database/backups/app_db_before_clear_activity_*.db

# 백업에서 복원
cp database/backups/app_db_before_clear_activity_YYYYMMDD_HHMMSS.db database/app.db
chown ubuntu:ubuntu database/app.db
chmod 644 database/app.db

# 서비스 재시작
sudo systemctl restart rohatax
```

---

## 📝 요약

1. **스크립트 실행**: `python3 scripts/clear_activity_logs_only.py`
2. **자동 백업**: 실행 전 자동으로 백업 생성
3. **안전 삭제**: activity_logs만 삭제, 유저 정보 보존
4. **확인**: 유저 정보가 그대로인지 확인

**유저 정보와 시스템 로직은 절대 건들지 않습니다!**

