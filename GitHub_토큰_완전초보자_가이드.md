# GitHub 토큰 완전 초보자 가이드

**대상**: GitHub 토큰을 처음 접하는 분들  
**목적**: 배포 서버에서 Git 태그를 푸시하기 위한 인증

---

## 🤔 토큰이란?

### 간단한 설명
**토큰(Token)**은 GitHub에 접근할 수 있는 **"비밀번호 같은 것"**입니다.

### 왜 필요한가요?
- 예전에는 GitHub 비밀번호로 로그인할 수 있었습니다
- 하지만 2021년부터는 보안을 위해 비밀번호 사용을 중단했습니다
- 대신 **Personal Access Token**을 사용해야 합니다

### 비유로 설명하면
- **비밀번호**: 집 열쇠 (더 이상 사용 불가)
- **토큰**: 특별한 열쇠 (새로운 방식)
- **목적**: GitHub에 접근해서 파일을 업로드하거나 다운로드하기

---

## 📋 토큰 만드는 방법 (단계별)

### 준비물
- GitHub 계정 (이미 있으시죠? kkr2652199-prog)
- 웹 브라우저 (Chrome, Edge 등)

---

### 1단계: GitHub 웹사이트 접속

1. 웹 브라우저를 엽니다
2. 주소창에 입력: `https://github.com`
3. Enter 키를 누릅니다
4. 로그인합니다 (계정: kkr2652199-prog)

---

### 2단계: Settings(설정) 들어가기

1. GitHub 페이지 우측 **상단**을 봅니다
2. **프로필 사진**을 찾습니다 (작은 원형 아이콘)
3. 프로필 사진을 **클릭**합니다
4. 나오는 메뉴에서 **"Settings"**를 클릭합니다

**참고**: Settings는 한국어로 "설정"입니다

---

### 3단계: Developer settings(개발자 설정) 찾기

1. Settings 페이지가 열립니다
2. **왼쪽 메뉴**를 봅니다
3. 맨 **아래쪽**으로 스크롤합니다
4. **"Developer settings"**를 찾아 클릭합니다

**참고**: 
- Developer settings는 한국어로 "개발자 설정"입니다
- 페이지 맨 아래에 있습니다

---

### 4단계: Personal access tokens(개인 액세스 토큰) 들어가기

1. Developer settings 페이지가 열립니다
2. **"Personal access tokens"**를 찾습니다
3. **"Tokens (classic)"**를 클릭합니다

**참고**: 
- Personal access tokens는 "개인 액세스 토큰"입니다
- Tokens (classic)는 "토큰 (클래식)"입니다

---

### 5단계: 새 토큰 생성하기

1. **"Generate new token"** 버튼을 찾습니다
2. 클릭하면 메뉴가 나옵니다
3. **"Generate new token (classic)"**를 클릭합니다

**참고**: 
- Generate new token은 "새 토큰 생성"입니다
- Generate new token (classic)는 "새 토큰 생성 (클래식)"입니다

---

### 6단계: 토큰 설정하기

화면에 여러 입력칸이 나타납니다:

#### Note (메모)
- **입력**: `배포서버 태그 푸시`
- **의미**: 이 토큰이 무엇을 위한 것인지 기록

#### Expiration (만료 기간)
- **선택**: `90 days` 또는 원하는 기간
- **의미**: 이 토큰이 언제까지 사용 가능한지

#### 권한 선택 (Scopes)
- **중요**: `repo` 체크박스를 찾습니다
- **체크**: `repo` 옆의 체크박스를 **클릭**합니다
- **의미**: 저장소(repository)에 접근할 수 있는 권한

**주의**: 
- `repo`만 체크하면 됩니다
- 다른 것은 체크하지 마세요

---

### 7단계: 토큰 생성 완료

1. 화면 맨 **아래**로 스크롤합니다
2. **"Generate token"** 버튼을 클릭합니다
3. 잠시 기다립니다
4. **토큰이 생성됩니다!**

---

### 8단계: 토큰 복사하기 (매우 중요!)

1. 화면에 **긴 글자**가 나타납니다
   - 예: `ghp_abc123xyz456def789ghi012jkl345mno678pqr901`
2. 이 글자가 **토큰**입니다
3. **전체를 복사**합니다
   - 방법: 마우스로 드래그해서 선택 → Ctrl+C (또는 우클릭 → 복사)

**⚠️ 매우 중요:**
- 이 토큰은 **한 번만** 보여줍니다
- 페이지를 닫으면 다시 볼 수 없습니다
- **지금 복사해두세요!**

---

## 📝 토큰 사용 방법

### 배포 서버에서 사용

배포 서버 터미널에서:

```bash
# 1. 원격 저장소 URL 변경
# [복사한_토큰] 부분을 방금 복사한 토큰으로 교체하세요
git remote set-url origin https://[복사한_토큰]@github.com/kkr2652199-prog/RohaTax-App.git
```

**예시:**
```bash
# 토큰이 ghp_abc123xyz456def789 라면
git remote set-url origin https://ghp_abc123xyz456def789@github.com/kkr2652199-prog/RohaTax-App.git
```

```bash
# 2. 태그 푸시
git push origin --tags
```

---

## 🎯 요약

1. **토큰이란?** → GitHub에 접근할 수 있는 비밀번호 같은 것
2. **왜 필요한가?** → GitHub가 비밀번호 사용을 중단했기 때문
3. **어디서 만드나?** → GitHub 웹사이트 → Settings → Developer settings → Personal access tokens
4. **어떻게 쓰나?** → 배포 서버에서 `git remote set-url` 명령어에 사용

---

## 🆘 문제 해결

### 문제: "Developer settings"를 찾을 수 없어요
- **해결**: Settings 페이지 맨 아래로 스크롤하세요

### 문제: "repo" 체크박스를 찾을 수 없어요
- **해결**: Scopes 섹션에서 찾으세요. "repo"는 "Repository access" 관련입니다

### 문제: 토큰을 복사하지 못했어요
- **해결**: 토큰을 다시 생성해야 합니다. 위 과정을 다시 따라하세요

### 문제: 토큰이 너무 길어요
- **해결**: 정상입니다. 토큰은 보통 40자 이상입니다. 전체를 복사하세요

---

**이제 토큰을 만들 준비가 되셨나요?**
**만들면서 막히는 부분이 있으면 알려주세요!**

