# 배포 서버 복구 명령어 - 스마트폰 최적화 전 상태로 되돌리기

## 스마트폰 최적화 전 상태로 되돌리기 (04d2c05)

배포 서버 터미널에서 다음 명령어를 순서대로 실행하세요:

```bash
cd /home/ubuntu/RohaTax-App
git checkout 04d2c05
sudo pkill -9 python3
source venv/bin/activate
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > /tmp/flask.log 2>&1 &
sleep 5
tail -30 /tmp/flask.log
lsof -i :5000
```

이 커밋은 스마트폰 최적화 작업 이전 상태입니다.

