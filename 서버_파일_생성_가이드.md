# 📝 서버에서 Python 파일 생성 가이드

> **중요**: 복사해야 할 부분과 설명 부분을 명확히 구분했습니다.

---

## 🎯 현재 단계: nano 에디터에 코드 붙여넣기

### ✅ 1단계: Python 코드 복사

**아래 전체 코드를 복사하세요!**

```
==========================================
아래부터 여기까지 전체 복사하세요
==========================================
```

<--- 복사하세요
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import sqlite3
import os
from pathlib import Path

project_root = Path(__file__).parent.parent
DB_PATH = project_root / 'database' / 'app.db'

def seed_products():
    print("=" * 60)
    print("🚀 상품 데이터 삽입 시작")
    print("=" * 60)
    
    if not DB_PATH.exists():
        print(f"❌ 데이터베이스 파일을 찾을 수 없습니다: {DB_PATH}")
        return
    
    try:
        conn = sqlite3.connect(str(DB_PATH))
        conn.row_factory = sqlite3.Row
        c = conn.cursor()
        
        print("\n[1단계] 기존 상품 데이터 확인 중...")
        existing = c.execute("SELECT COUNT(*) as count FROM products").fetchone()
        print(f"   기존 상품 수: {existing['count']}개")
        
        products = [
            ('Welcome Event', '신규 가입 혜택 (50토큰)', 0, 50, 0, 'event', 1),
            ('Welcome Period Event', '신규 가입 혜택 (3일 무료)', 0, 0, 3, 'event_period', 1),
            ('Standard', '필요할 때만 사용하는 유연한 플랜', 300, 1, 0, 'package', 1),
            ('Premium', '100건 패키지로 한 번에 해결', 15000, 100, 0, 'package', 1),
            ('Gold', '세무사/대리 발급 전문', 100000, 999999, 30, 'subscription', 1),
        ]
        
        print("\n[2단계] 상품 데이터 삽입 중...")
        inserted_count = 0
        skipped_count = 0
        
        for p in products:
            name, description, price, token_amount, duration_days, product_type, is_active = p
            
            c.execute('SELECT id FROM products WHERE name = ?', (name,))
            if c.fetchone():
                print(f"   ⚠️  건너뜀: {name} (이미 존재)")
                skipped_count += 1
                continue
            
            c.execute(
                """INSERT INTO products 
                   (name, description, price, token_amount, duration_days, type, is_active, vat_included, created_at, updated_at) 
                   VALUES (?, ?, ?, ?, ?, ?, ?, 0, datetime('now', 'localtime'), datetime('now', 'localtime'))""",
                (name, description, price, token_amount, duration_days, product_type, is_active)
            )
            print(f"   ✅ 삽입 완료: {name}")
            inserted_count += 1
        
        conn.commit()
        
        print("\n[3단계] 삽입된 데이터 확인...")
        rows = c.execute(
            "SELECT id, name, price, token_amount, type, is_active FROM products ORDER BY id"
        ).fetchall()
        
        print("\n" + "=" * 60)
        print("📊 삽입된 상품 목록:")
        print("-" * 60)
        for row in rows:
            status = "활성" if row['is_active'] else "비활성"
            print(f"ID: {row['id']:2d} | {row['name']:20s} | {row['price']:8d}원 | {row['token_amount']:6d}토큰 | {row['type']:15s} | {status}")
        print("-" * 60)
        print(f"\n✅ 완료!")
        print(f"   - 새로 삽입: {inserted_count}개")
        print(f"   - 건너뜀: {skipped_count}개")
        print(f"   - 전체 상품: {len(rows)}개")
        print("=" * 60)
        
        conn.close()
        
    except Exception as e:
        print(f"\n❌ 오류 발생: {str(e)}")
        import traceback
        traceback.print_exc()

if __name__ == '__main__':
    seed_products()
<--- 복사 끝

```
==========================================
여기까지 전체 복사하세요
==========================================
```

### <--- 복사하지 마세요 (설명)
위의 `<--- 복사하세요`부터 `<--- 복사 끝`까지의 **모든 내용**을 복사하세요.
첫 번째 줄(`#!/usr/bin/env python3`)부터 마지막 줄(`seed_products()`)까지 전체를 복사해야 합니다.
### <--- 복사하지 마세요 (설명 끝)

---

### ✅ 2단계: nano 에디터에 붙여넣기

<--- 복사하지 마세요 (설명)
nano 에디터 창을 클릭한 후, 복사한 코드를 붙여넣으세요.
### <--- 복사하지 마세요 (설명 끝)

**붙여넣기 방법:**
- 마우스 우클릭 → 붙여넣기
- 또는 Ctrl+Shift+V (브라우저 터미널)
- 또는 Shift+Insert

---

### ✅ 3단계: 파일 저장

<--- 복사하지 마세요 (설명)
코드가 붙여넣어진 것을 확인한 후 파일을 저장하세요.
### <--- 복사하지 마세요 (설명 끝)

**저장 방법:**
1. **Ctrl+O** (저장)
2. **Enter** (파일명 확인)
3. "Wrote X lines" 메시지 확인

---

### ✅ 4단계: nano 종료

<--- 복사하지 마세요 (설명)
파일 저장 후 nano 에디터를 종료하세요.
### <--- 복사하지 마세요 (설명 끝)

**종료 방법:**
1. **Ctrl+X** (종료)
2. 터미널 프롬프트로 돌아옴

---

### ✅ 5단계: 스크립트 실행

<--- 복사하지 마세요 (설명)
터미널 프롬프트에서 스크립트를 실행하세요.
### <--- 복사하지 마세요 (설명 끝)

<--- 복사하세요
python3 scripts/seed_products_server.py
<--- 복사 끝

---

## 📋 체크리스트

- [ ] 1단계: Python 코드 전체 복사 완료
- [ ] 2단계: nano 에디터에 붙여넣기 완료
- [ ] 3단계: 파일 저장 완료 (Ctrl+O → Enter)
- [ ] 4단계: nano 종료 완료 (Ctrl+X)
- [ ] 5단계: 스크립트 실행 완료

---

## ⚠️ 중요 안내

### 복사 범위
- **복사해야 할 부분**: `<--- 복사하세요`와 `<--- 복사 끝` 사이의 **모든 내용**
- **복사하지 말아야 할 부분**: `<--- 복사하지 마세요`로 시작하는 설명 부분

### 예시
```
<--- 복사하세요
이 줄부터
이 줄까지
모두 복사하세요
<--- 복사 끝

<--- 복사하지 마세요 (설명)
이 부분은 설명이므로 복사하지 마세요
### <--- 복사하지 마세요 (설명 끝)
```

---

**작성일**: 2025-12-27  
**작성자**: Auto (Cursor AI Assistant)  
**프로젝트**: RohaTax homepage1




