# 🌐 인터넷 서버 100% 동작 보장 가이드

## 📅 작성일: 2025-12-27
## 🎯 목표: 로컬과 동일하게 100% 작동하는 인터넷 서버 구축

---

## 🔴 Critical: 24시간 자동 실행 설정

### 1. Systemd 서비스 파일 생성 (필수)

**파일 위치**: `/etc/systemd/system/rohatax.service`

```ini
[Unit]
Description=RohaTax Flask Application
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/RohaTax-App
Environment="PATH=/home/ubuntu/RohaTax-App/venv/bin:/usr/local/bin:/usr/bin:/bin"
Environment="FLASK_RUN_HOST=0.0.0.0"
Environment="FLASK_RUN_PORT=80"
EnvironmentFile=/home/ubuntu/RohaTax-App/.env
ExecStart=/home/ubuntu/RohaTax-App/venv/bin/python3 /home/ubuntu/RohaTax-App/app.py
Restart=always
RestartSec=10
StandardOutput=append:/home/ubuntu/RohaTax-App/logs/app_service.log
StandardError=append:/home/ubuntu/RohaTax-App/logs/app_service_error.log

[Install]
WantedBy=multi-user.target
```

**설정 방법:**
```bash
# 1. 서비스 파일 생성
sudo nano /etc/systemd/system/rohatax.service
# (위 내용 복사)

# 2. 서비스 활성화
sudo systemctl daemon-reload
sudo systemctl enable rohatax.service
sudo systemctl start rohatax.service

# 3. 상태 확인
sudo systemctl status rohatax.service
```

**장점:**
- ✅ 서버 재부팅 시 자동 시작
- ✅ 프로세스 크래시 시 자동 재시작
- ✅ 로그 자동 관리
- ✅ 시스템 리소스 관리

---

## 🟡 Important: 로컬-서버 환경 차이 해결

### 2. 환경 차이점 체크리스트

#### A. 데이터베이스 차이
- [ ] **테이블 구조 동일성 확인**
  ```bash
  # 로컬
  python -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\"'); print('\n'.join([r[0] for r in cursor.fetchall()]))"
  
  # 서버
  python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\"'); print('\n'.join([r[0] for r in cursor.fetchall()]))"
  ```

- [ ] **초기 데이터 동일성 확인**
  ```bash
  # products 테이블 데이터 확인
  python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM products'); print('Products:', cursor.fetchone()[0])"
  ```

#### B. 환경 변수 차이
- [ ] **.env 파일 비교**
  ```bash
  # 로컬 .env 내용 확인
  # 서버 .env 내용 확인
  # 차이점 기록
  ```

#### C. 정적 파일 차이
- [ ] **CSS/JS 파일 존재 확인**
  ```bash
  # 서버에서 확인
  find static -name "*.css" -o -name "*.js" | wc -l
  ```

#### D. 파일 권한 차이
- [ ] **디렉토리 권한 확인**
  ```bash
  ls -la database/
  ls -la static/
  ls -la uploads/
  ls -la output/
  ```

---

## 🟢 Optional: 모니터링 및 자동 복구

### 3. 헬스체크 스크립트

**파일**: `scripts/health_check.sh`

```bash
#!/bin/bash
# 서버 헬스체크 스크립트

URL="http://localhost:80"
RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" $URL)

if [ "$RESPONSE" != "200" ]; then
    echo "❌ 서버 응답 실패: $RESPONSE"
    sudo systemctl restart rohatax.service
    # 알림 전송 (선택)
else
    echo "✅ 서버 정상: $RESPONSE"
fi
```

**Cron 설정 (5분마다 체크):**
```bash
# crontab -e
*/5 * * * * /home/ubuntu/RohaTax-App/scripts/health_check.sh >> /home/ubuntu/RohaTax-App/logs/health_check.log 2>&1
```

---

## 📋 로컬-서버 기능 동작 비교 체크리스트

### 기능별 테스트

#### 1. 홈페이지 접속
- [ ] 로컬: `http://localhost:5000` ✅
- [ ] 서버: `https://rohatax.com` ❓
- **차이점**: 

#### 2. 로그인 기능
- [ ] 로컬: 로그인 성공 ✅
- [ ] 서버: 로그인 성공 ❓
- **차이점**: 

#### 3. 파일 업로드
- [ ] 로컬: 업로드 성공 ✅
- [ ] 서버: 업로드 성공 ❓
- **차이점**: 

#### 4. 변환 기능
- [ ] 로컬: 변환 성공 ✅
- [ ] 서버: 변환 성공 ❓
- **차이점**: 

#### 5. 정적 파일 로딩
- [ ] 로컬: CSS/JS 로딩 ✅
- [ ] 서버: CSS/JS 로딩 ❓
- **차이점**: 

---

## 🔧 자동화 스크립트

### 4. 로컬-서버 환경 비교 스크립트

**파일**: `scripts/compare_local_server.py`

```python
#!/usr/bin/env python3
"""로컬과 서버 환경 비교 스크립트"""
import sqlite3
import os
from pathlib import Path

def compare_databases():
    """데이터베이스 비교"""
    local_db = Path("database/app.db")
    # 서버 DB는 SSH로 접근 필요
    
def compare_env():
    """환경 변수 비교"""
    # .env 파일 비교
    
def compare_static_files():
    """정적 파일 비교"""
    # static 디렉토리 파일 목록 비교
```

---

## 🚀 실행 계획

### 즉시 실행 (오늘)

1. **Systemd 서비스 설정**
   - 서비스 파일 생성
   - 자동 시작 활성화
   - 상태 확인

2. **환경 차이점 점검**
   - 데이터베이스 비교
   - 환경 변수 비교
   - 정적 파일 비교

3. **기능 테스트**
   - 각 기능별 로컬-서버 비교
   - 차이점 기록

### 이번 주 (필수)

1. **차이점 해결**
   - 데이터베이스 동기화
   - 환경 변수 동기화
   - 파일 권한 수정

2. **모니터링 설정**
   - 헬스체크 스크립트
   - 자동 재시작 설정

---

## ✅ 체크리스트 요약

### 🔴 Critical (즉시)
- [ ] Systemd 서비스 파일 생성
- [ ] 자동 시작 활성화
- [ ] 데이터베이스 테이블 비교
- [ ] 환경 변수 비교

### 🟡 Important (1주일)
- [ ] 기능별 테스트 및 비교
- [ ] 차이점 해결
- [ ] 헬스체크 설정

### 🟢 Optional (1개월)
- [ ] 모니터링 강화
- [ ] 알림 시스템
- [ ] 성능 최적화

---

**목표: 로컬과 100% 동일하게 작동하는 인터넷 서버!** 🎯

