# 인터넷 서버 실행 및 업데이트 명령어 (Lightsail 터미널용)

## 📋 서버 실행 방법

### 1단계: 기존 프로세스 종료
<--- 복사하세요
lsof -ti:5000 | xargs kill -9 2>/dev/null || true

<--- 복사하세요
lsof -ti:5001 | xargs kill -9 2>/dev/null || true

### 2단계: 서버 시작
<--- 복사하세요
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 &

### 3단계: 실행 확인
<--- 복사하세요
sleep 3 && tail -10 app.log

<--- 복사하세요
lsof -i :5000

---

## 🔄 업데이트 내용 적용 방법

### 방법 1: Git으로 업데이트 받기 (권장)

#### 1단계: 변경사항 가져오기
<--- 복사하세요
git pull origin main

#### 2단계: 서버 재시작
<--- 복사하세요
lsof -ti:5000 | xargs kill -9 2>/dev/null || true && FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 & && sleep 3 && tail -10 app.log

---

### 방법 2: 특정 파일만 수정하기

#### app.py 수정 (호스트/포트 변경)
<--- 복사하세요
sed -i 's/host = os.environ.get("FLASK_RUN_HOST", "127.0.0.1")/host = os.environ.get("FLASK_RUN_HOST", "0.0.0.0")/g' app.py && sed -i 's/port = int(os.environ.get("FLASK_RUN_PORT", settings.PORT))/port = int(os.environ.get("FLASK_RUN_PORT", 5000))/g' app.py && echo "✅ 수정 완료!"

#### 수정 확인
<--- 복사하세요
tail -3 app.py

---

## 🛠️ 유용한 명령어

### 서버 상태 확인
<--- 복사하세요
ps aux | grep python3

### 로그 실시간 확인
<--- 복사하세요
tail -f app.log

### 포트 사용 확인
<--- 복사하세요
lsof -i :5000

### 서버 중지
<--- 복사하세요
lsof -ti:5000 | xargs kill -9 2>/dev/null || true

---

## 📝 전체 업데이트 및 재시작 (한 번에)

<--- 복사하세요
git pull origin main && lsof -ti:5000 | xargs kill -9 2>/dev/null || true && FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 & && sleep 3 && tail -10 app.log && lsof -i :5000

이 명령어는:
1. Git에서 최신 코드 가져오기
2. 기존 서버 종료
3. 새 서버 시작
4. 로그 확인
5. 포트 확인

을 한 번에 실행합니다.

---

## 🔧 권한 문제 해결 (Permission denied 오류 발생 시)

**⚠️ 중요: 명령어는 하나씩 실행하세요!**

### _pycache_ 삭제 후 재시작 (단계별)

#### 1단계: 기존 서버 종료
<--- 복사하세요
lsof -ti:5000 | xargs kill -9 2>/dev/null || true

#### 2단계: _pycache_ 삭제 (권한 오류는 무시해도 됩니다)
<--- 복사하세요
find . -type d -name "_pycache_" -exec rm -r {} + 2>/dev/null || true

#### 3단계: .pyc 파일 삭제
<--- 복사하세요
find . -type f -name "*.pyc" -delete 2>/dev/null || true

#### 4단계: 서버 시작
<--- 복사하세요
cd /home/ubuntu/RohaTax-App

<--- 복사하세요
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 &

#### 5단계: 잠시 대기 후 로그 확인
<--- 복사하세요
sleep 5

<--- 복사하세요
tail -20 app.log

#### 6단계: 포트 확인
<--- 복사하세요
lsof -i :5000

---

## 🌐 웹사이트 접근 확인

### 1단계: 서버에서 직접 확인 (로컬)
<--- 복사하세요
curl -I http://localhost:5000

**예상 결과:** `HTTP/1.1 200 OK` ✅

### 2단계: 서버 외부 IP로 확인 (공개 IP)
<--- 복사하세요
curl -I http://52.78.116.159:5000

**예상 결과:** `HTTP/1.1 200 OK` ✅

**만약 연결이 안 되면:** Lightsail 방화벽에서 포트 5000이 열려있는지 확인하세요.

### 3단계: Cloudflare 도메인 확인
브라우저에서 `https://rohatax.com` 접속해보세요

**예상 결과:** 웹사이트가 정상적으로 표시되어야 합니다.

**만약 Error 521이 계속 나오면:** 아래 "Error 521 해결 방법" 섹션을 참고하세요.

---

## 🔥 Error 521 해결 방법

**Error 521 = Cloudflare가 원본 서버에 연결할 수 없음**

### ⚠️ 가장 흔한 원인: Lightsail 방화벽 설정

**증상:** 
- 로컬 접근 성공: `curl http://localhost:5000` → `200 OK` ✅
- 공개 IP 접근 실패: `curl http://52.78.116.159:5000` → `Failed to connect` ❌

**해결 방법:**

#### Lightsail 방화벽 설정 (필수!)

1. **AWS Lightsail 콘솔 접속**
   - https://lightsail.aws.amazon.com 접속
   - 인스턴스 선택: `roha-tax-server`

2. **네트워킹 탭 클릭**

3. **방화벽 섹션 확인**
   - 현재 열려있는 포트 목록 확인
   - **포트 5000이 없으면 추가 필요!**

4. **포트 5000 추가하기**
   - "방화벽 규칙 추가" 또는 "Add rule" 클릭
   - 설정:
     - **애플리케이션:** Custom
     - **프로토콜:** TCP
     - **포트 범위:** 5000
     - **소스:** Anywhere (0.0.0.0/0)
   - 저장

5. **방화벽 설정 후 테스트**
   - 몇 초 대기 (방화벽 규칙 적용 시간)
   - Lightsail 터미널에서 다시 테스트:
   
<--- 복사하세요
```
curl -I http://52.78.116.159:5000
```

**예상 결과:** `HTTP/1.1 200 OK` ✅

---

### 기타 확인 사항:

#### 1. 서버가 실행 중인지 확인
<--- 복사하세요
ps aux | grep python3

#### 2. 포트 5000이 열려있는지 확인
<--- 복사하세요
lsof -i :5000

#### 3. Cloudflare DNS 설정 확인 (중요!)

**⚠️ 핵심 문제: Cloudflare는 "프록시됨" 모드일 때 포트 80/443만 지원합니다!**

**옵션 A: Cloudflare 프록시 모드 변경 (포트 5000 사용하려면)**

1. Cloudflare 대시보드 → DNS → 레코드
2. `rohatax.com` A 레코드 찾기
3. **프록시 상태를 "DNS 전용" (회색 구름)으로 변경**
   - 이렇게 하면 `https://rohatax.com:5000`으로 접속 가능
   - 하지만 포트 번호를 URL에 포함해야 함

**옵션 B: Flask를 포트 80으로 실행 (권장!)**

Cloudflare "프록시됨" 모드를 유지하면서 `https://rohatax.com`으로 접속하려면:

1. Flask 앱을 포트 80으로 실행해야 함
2. 하지만 포트 80은 root 권한 필요
3. 또는 포트 443 (HTTPS) 사용

**옵션 C: 포트 전환 (80 → 5000)**

Nginx나 다른 리버스 프록시를 사용하여 포트 80을 포트 5000으로 전환

---

### 🎯 가장 간단한 해결 방법: Cloudflare DNS 설정 확인

1. **Cloudflare 대시보드 접속**
   - https://dash.cloudflare.com 접속
   - `rohatax.com` 선택

2. **DNS → 레코드 탭 클릭**

3. **A 레코드 확인**
   - 이름: `@` 또는 `rohatax.com`
   - IPv4 주소: `52.78.116.159`
   - **프록시 상태 확인:**
     - **"프록시됨" (주황색 구름)**:** 포트 80/443만 지원 → 포트 5000 접속 불가
     - **"DNS 전용" (회색 구름)**:** 모든 포트 지원 → 포트 5000 접속 가능

4. **포트 5000을 사용하려면:**
   - 프록시 상태를 "DNS 전용"으로 변경
   - 브라우저에서 `http://rohatax.com:5000` 또는 `https://rohatax.com:5000` 접속

5. **포트 번호 없이 접속하려면:**
   - Flask를 포트 80으로 실행 (root 권한 필요)
   - 또는 Nginx 리버스 프록시 설정

---

#### 4. Cloudflare SSL/TLS 설정 확인
- SSL/TLS → 개요 → 암호화 모드: **"유연한" 또는 "전체"**로 설정 ✅ (이미 완료)
- SSL/TLS → Edge Certificates → 항상 HTTPS 사용: **켜기**

---

## ⚠️ 주의사항

- 서버를 재시작하면 잠깐 서비스가 중단될 수 있습니다
- `app.log` 파일에서 오류를 확인할 수 있습니다
- `lsof -i :5000` 명령어로 서버가 정상 실행 중인지 확인하세요
- 권한 오류가 발생하면 위의 "권한 문제 해결" 섹션을 참고하세요

