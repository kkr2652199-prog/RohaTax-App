# 🔍 로컬 vs 서버 차이점 완전 분석

## 📋 목표
로컬에서 100% 작동하는 구조를 인터넷 서버로 완벽하게 옮기기

---

## 🎯 체계적 접근 방법

### 1단계: 로컬 환경 완전 분석
### 2단계: 서버 환경 완전 분석  
### 3단계: 차이점 목록 작성
### 4단계: 자동화 스크립트로 한 번에 해결

---

## 📊 로컬 환경 분석 체크리스트

### A. 데이터베이스 상태
- [ ] 데이터베이스 파일 위치: `database/app.db`
- [ ] 테이블 목록 확인
- [ ] 초기 데이터 확인 (products, users 등)
- [ ] 마이그레이션 상태 확인

**확인 명령어 (로컬 PowerShell):**
```powershell
cd C:\Users\user\Desktop\RohaTax\homepage1
python -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\" ORDER BY name'); print('\n'.join([row[0] for row in cursor.fetchall()]))"
```

### B. 환경 변수 (.env 파일)
- [ ] SECRET_KEY 설정 여부
- [ ] ENVIRONMENT 설정
- [ ] DEBUG 설정
- [ ] 기타 환경 변수

**확인 명령어 (로컬 PowerShell):**
```powershell
cat .env
```

### C. 정적 파일
- [ ] CSS 파일 위치 및 존재
- [ ] JavaScript 파일 위치 및 존재
- [ ] 이미지 파일 위치 및 존재
- [ ] 비디오 파일 위치 및 존재

**확인 명령어 (로컬 PowerShell):**
```powershell
Get-ChildItem -Recurse static | Select-Object FullName | Format-Table -AutoSize
```

### D. 의존성 패키지
- [ ] requirements.txt 존재 여부
- [ ] 모든 패키지 버전 확인

**확인 명령어 (로컬 PowerShell):**
```powershell
pip freeze > requirements_check.txt
cat requirements.txt
```

### E. 파일 권한 및 구조
- [ ] 디렉토리 구조
- [ ] 실행 권한이 필요한 스크립트

---

## 📊 서버 환경 분석 체크리스트

### A. 데이터베이스 상태
- [ ] 데이터베이스 파일 존재 여부
- [ ] 테이블 목록 확인
- [ ] 초기 데이터 확인

**확인 명령어 (서버):**
<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT name FROM sqlite_master WHERE type=\"table\" ORDER BY name'); print('\n'.join([row[0] for row in cursor.fetchall()]))"
```

### B. 환경 변수 (.env 파일)
- [ ] .env 파일 존재 여부
- [ ] SECRET_KEY 설정 여부
- [ ] 모든 환경 변수 확인

**확인 명령어 (서버):**
<--- 복사하세요
```
cat .env
```

### C. 정적 파일
- [ ] 모든 정적 파일 존재 여부
- [ ] 파일 경로 확인

**확인 명령어 (서버):**
<--- 복사하세요
```
find static -type f | head -20
```

### D. 의존성 패키지
- [ ] requirements.txt 존재 여부
- [ ] 설치된 패키지 확인

**확인 명령어 (서버):**
<--- 복사하세요
```
pip3 list
cat requirements.txt
```

---

## 🔄 차이점 분석 및 해결

### 차이점 1: 데이터베이스 스키마
**로컬:** 모든 테이블 존재, 초기 데이터 있음
**서버:** 일부 테이블 누락 (orders 등), 초기 데이터 없음

**해결:** `deploy_server.py` 스크립트로 자동 생성

### 차이점 2: 환경 변수
**로컬:** .env 파일에 SECRET_KEY 등 설정됨
**서버:** .env 파일 없거나 불완전

**해결:** `server_setup.sh` 스크립트로 자동 생성

### 차이점 3: 정적 파일
**로컬:** 모든 파일 존재
**서버:** 일부 파일 누락 (비디오 등)

**해결:** Git으로 동기화 또는 수동 업로드

### 차이점 4: 파일 권한
**로컬:** Windows 권한 시스템
**서버:** Linux 권한 시스템 (ubuntu 사용자)

**해결:** `server_setup.sh` 스크립트로 자동 수정

---

## 🚀 완벽한 배포 프로세스

### 단계 1: 로컬에서 분석 스크립트 실행

**로컬 PowerShell에서:**
```powershell
cd C:\Users\user\Desktop\RohaTax\homepage1
python scripts/analyze_local_environment.py
```

이 스크립트는:
- 데이터베이스 스키마 추출
- 환경 변수 목록 추출
- 정적 파일 목록 추출
- 의존성 패키지 목록 추출

### 단계 2: 서버에서 동기화 스크립트 실행

**서버에서:**
<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
git pull origin main
bash scripts/server_setup.sh
```

이 스크립트는:
- 데이터베이스 동기화 (로컬과 동일하게)
- 환경 변수 동기화
- 파일 권한 수정
- 서버 재시작

---

## 📝 체크리스트: 로컬과 서버 동일성 확인

배포 후 확인:

### 데이터베이스
- [ ] 테이블 개수 동일
- [ ] 초기 데이터 동일
- [ ] 스키마 동일

### 환경 변수
- [ ] SECRET_KEY 설정됨
- [ ] ENVIRONMENT=production
- [ ] DEBUG=False

### 정적 파일
- [ ] 모든 CSS 파일 존재
- [ ] 모든 JS 파일 존재
- [ ] 모든 이미지 파일 존재
- [ ] 모든 비디오 파일 존재

### 기능 테스트
- [ ] 로그인 작동
- [ ] 상품 구매 작동
- [ ] 변환 기능 작동
- [ ] 관리자 대시보드 작동

---

## 🎯 결론

**현재 방식 (수동):**
- 하나씩 발견하고 수정 → 비효율적, 실수 가능

**권장 방식 (자동화):**
1. 로컬 환경 완전 분석
2. 서버 환경 완전 분석
3. 차이점 자동 감지
4. 자동화 스크립트로 한 번에 해결

이렇게 하면 로컬과 서버가 100% 동일하게 작동합니다.

