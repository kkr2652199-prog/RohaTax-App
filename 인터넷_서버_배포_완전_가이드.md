# 🌐 RohaTax 인터넷 서버 배포 완전 가이드

## 📋 목표
로컬에서 개발한 Flask 애플리케이션을 인터넷에서 접근 가능하도록 배포하기

---

## 🎯 전체 프로세스 개요

```
1. 서버 준비 (Lightsail 인스턴스)
   ↓
2. 코드 배포 (Git으로 코드 업로드)
   ↓
3. 서버 환경 설정 (Python, 가상환경, 패키지 설치)
   ↓
4. 데이터베이스 설정 (SQLite 또는 PostgreSQL)
   ↓
5. Flask 서버 실행 (포트 설정)
   ↓
6. 방화벽 설정 (Lightsail)
   ↓
7. DNS 설정 (Cloudflare)
   ↓
8. SSL/TLS 설정 (Cloudflare)
   ↓
9. 최종 테스트
```

---

## 1단계: 서버 준비 (AWS Lightsail)

### 1-1. Lightsail 인스턴스 생성
- ✅ 이미 완료: `roha-tax-server` (IP: `52.78.116.159`)

### 1-2. 기본 방화벽 규칙 확인
- ✅ SSH (포트 22): 열림
- ✅ HTTP (포트 80): 열림
- ✅ HTTPS (포트 443): 열림
- ⚠️ 커스텀 포트 (5000): 필요시 추가

---

## 2단계: 코드 배포

### 2-1. 로컬에서 Git 저장소 확인
```bash
# 로컬 터미널 (PowerShell)
cd C:\Users\user\Desktop\RohaTax\homepage1
git status
git push origin main  # 하늘저장소로 푸시
```

### 2-2. 서버에서 코드 가져오기
```bash
# Lightsail 터미널
cd /home/ubuntu
git clone <하늘저장소_URL> RohaTax-App
# 또는 이미 있다면
cd /home/ubuntu/RohaTax-App
git pull origin main
```

---

## 3단계: 서버 환경 설정

### 3-1. Python 가상환경 생성 및 활성화
```bash
cd /home/ubuntu/RohaTax-App
python3 -m venv venv
source venv/bin/activate
```

### 3-2. 필수 패키지 설치
```bash
pip install --upgrade pip
pip install -r requirements.txt
# requirements.txt가 없다면
pip install flask python-dotenv
```

---

## 4단계: 데이터베이스 설정

### 4-1. 데이터베이스 디렉토리 생성
```bash
mkdir -p /home/ubuntu/RohaTax-App/database
```

### 4-2. 초기 데이터 삽입 (필요시)
```bash
cd /home/ubuntu/RohaTax-App
python3 scripts/seed_products_server.py
```

---

## 5단계: Flask 서버 실행 설정

### 5-1. app.py 확인
- `app.py` 파일이 올바른 호스트/포트 설정을 가지고 있는지 확인
- 기본값: `host="0.0.0.0"`, `port=5000`

### 5-2. 서버 실행 방법 선택

#### 옵션 A: 포트 5000 사용 (개발/테스트용)
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 &
```

**장점:**
- 설정 간단
- root 권한 불필요

**단점:**
- URL에 포트 번호 필요 (`http://rohatax.com:5000`)
- Cloudflare 프록시와 호환성 문제 가능

#### 옵션 B: 포트 80 사용 (프로덕션 권장)
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
sudo FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=80 nohup python3 app.py > app.log 2>&1 &
```

**장점:**
- URL에 포트 번호 불필요 (`http://rohatax.com`)
- Cloudflare 프록시와 완벽 호환
- 표준 HTTP 포트

**단점:**
- root 권한 필요 (`sudo`)
- 보안상 주의 필요

---

## 6단계: Lightsail 방화벽 설정

### 6-1. 방화벽 규칙 확인
1. AWS Lightsail 콘솔 접속
2. 인스턴스 선택 → 네트워킹 탭
3. IPv4 방화벽 섹션 확인

### 6-2. 포트 추가 (필요시)

#### 포트 5000 사용하는 경우:
- 애플리케이션: Custom
- 프로토콜: TCP
- 포트 범위: 5000
- 소스: Anywhere (0.0.0.0/0)
- **중요:** "IP 주소로 제한하기" 체크 해제!

#### 포트 80 사용하는 경우:
- 이미 HTTP 규칙이 있으면 추가 불필요

---

## 7단계: DNS 설정 (Cloudflare)

### 7-1. Cloudflare 대시보드 접속
- https://dash.cloudflare.com
- `rohatax.com` 선택

### 7-2. DNS 레코드 설정
1. DNS → 레코드 탭
2. A 레코드 확인/추가:
   - **형식:** A
   - **이름:** `@` 또는 `rohatax.com`
   - **IPv4 주소:** `52.78.116.159`
   - **프록시 상태:** 선택 필요 (아래 참고)

### 7-3. 프록시 상태 선택

#### 옵션 A: "DNS 전용" (회색 구름) - 포트 5000 사용 시
- **장점:** 모든 포트 지원
- **단점:** URL에 포트 번호 필요 (`http://rohatax.com:5000`)
- **사용 시나리오:** 개발/테스트 단계

#### 옵션 B: "프록싱됨" (주황색 구름) - 포트 80 사용 시
- **장점:** URL에 포트 번호 불필요 (`http://rohatax.com`)
- **장점:** Cloudflare DDoS 보호, CDN 기능
- **단점:** 포트 80/443만 지원
- **사용 시나리오:** 프로덕션 배포

---

## 8단계: SSL/TLS 설정 (Cloudflare)

### 8-1. SSL/TLS 암호화 모드 설정
1. Cloudflare 대시보드 → SSL/TLS → 개요
2. 암호화 모드 선택:
   - **"유연한" (Flexible):** 권장 (서버에 SSL 인증서 불필요)
   - **"전체" (Full):** 서버에 SSL 인증서 필요

### 8-2. 항상 HTTPS 사용
1. SSL/TLS → Edge Certificates
2. "항상 HTTPS 사용" 켜기

---

## 9단계: 최종 테스트

### 9-1. 서버 상태 확인
```bash
# Lightsail 터미널
ps aux | grep python3
lsof -i :5000  # 또는 lsof -i :80
curl -I http://localhost:5000  # 또는 http://localhost:80
```

### 9-2. 공개 IP 접근 테스트
```bash
# Lightsail 터미널
curl -I http://52.78.116.159:5000  # 또는 :80
```

### 9-3. 도메인 접근 테스트
- 브라우저에서 접속:
  - 포트 5000 사용 시: `http://rohatax.com:5000`
  - 포트 80 사용 시: `http://rohatax.com` 또는 `https://rohatax.com`

---

## 🔄 현재 상황 정리 및 권장 사항

### 현재 상태
- ✅ 서버 실행 중 (포트 5000)
- ✅ Lightsail 방화벽 설정 완료
- ⚠️ Cloudflare DNS 설정 복잡함
- ⚠️ 포트 5000과 Cloudflare 프록시 호환성 문제

### 권장 해결책: 포트 80으로 전환

**이유:**
1. 표준 HTTP 포트 사용
2. Cloudflare 프록시와 완벽 호환
3. URL에 포트 번호 불필요
4. 프로덕션 환경에 적합

**전환 방법:**
1. 기존 서버 종료
2. 포트 80으로 재시작
3. Cloudflare 프록시 "프록싱됨" 유지
4. `https://rohatax.com`으로 접속

---

## 📝 체크리스트

배포 전 확인 사항:
- [ ] 서버에 코드 배포 완료
- [ ] Python 가상환경 설정 완료
- [ ] 필수 패키지 설치 완료
- [ ] 데이터베이스 설정 완료
- [ ] Flask 서버 실행 중
- [ ] Lightsail 방화벽 설정 완료
- [ ] Cloudflare DNS 설정 완료
- [ ] Cloudflare SSL/TLS 설정 완료
- [ ] 로컬 접근 테스트 성공
- [ ] 공개 IP 접근 테스트 성공
- [ ] 도메인 접근 테스트 성공

---

## 🆘 문제 해결

### Error 521 (웹 서버 다운)
- 서버가 실행 중인지 확인
- 방화벽에서 포트가 열려있는지 확인
- Cloudflare 프록시 모드와 서버 포트 일치 확인

### 연결 시간 초과
- Lightsail 방화벽 설정 확인
- 서버 포트 확인 (`lsof -i :포트번호`)

### DNS 전파 지연
- DNS 변경 후 1-2분 대기
- Cloudflare 캐시 지우기

---

## 🎯 다음 단계

배포 완료 후:
1. 모니터링 설정 (로그 확인)
2. 백업 자동화
3. 보안 강화 (SECRET_KEY 설정 등)
4. 성능 최적화
5. 프로덕션 WSGI 서버 고려 (Gunicorn, uWSGI)

