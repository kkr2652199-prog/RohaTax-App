# 🔴 Critical: 서버 24시간 자동 실행 설정

## 📅 작성일: 2025-12-27
## 🎯 문제: 로컬 PC가 꺼지면 인터넷 서버도 종료됨
## ✅ 해결: Systemd 서비스로 독립 실행 설정

---

## 🚨 현재 문제점

**현재 상황:**
- 인터넷 서버가 로컬 PC와 연결되어 있음
- 로컬 PC가 꺼지면 서버도 종료됨
- SSH 세션이 끊어지면 프로세스가 종료될 수 있음

**이유:**
- `nohup`으로 실행했지만 SSH 세션과 연결되어 있음
- Systemd 서비스로 설정되지 않음
- 서버 재부팅 시 자동 시작되지 않음

---

## ✅ 해결 방법: Systemd 서비스 설정

### 1단계: 서버에 SSH 접속

```bash
# AWS Lightsail 터미널 또는 SSH로 접속
ssh ubuntu@52.78.116.159
```

### 2단계: 프로젝트 디렉토리로 이동

```bash
cd /home/ubuntu/RohaTax-App
```

### 3단계: 기존 프로세스 종료

```bash
# 현재 실행 중인 프로세스 종료
pkill -f "python3 app.py" 2>/dev/null || true
lsof -ti:80 | xargs kill -9 2>/dev/null || true
lsof -ti:5000 | xargs kill -9 2>/dev/null || true
```

### 4단계: Systemd 서비스 파일 생성

```bash
# 스크립트 실행 권한 부여
chmod +x scripts/create_systemd_service.sh

# Systemd 서비스 파일 생성
bash scripts/create_systemd_service.sh
```

**또는 수동으로 생성:**

```bash
# 서비스 파일 생성
sudo nano /etc/systemd/system/rohatax.service
```

**파일 내용:**
```ini
[Unit]
Description=RohaTax Flask Application
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/RohaTax-App
Environment="PATH=/home/ubuntu/RohaTax-App/venv/bin:/usr/local/bin:/usr/bin:/bin"
Environment="FLASK_RUN_HOST=0.0.0.0"
Environment="FLASK_RUN_PORT=80"
EnvironmentFile=/home/ubuntu/RohaTax-App/.env
ExecStart=/home/ubuntu/RohaTax-App/venv/bin/python3 /home/ubuntu/RohaTax-App/app.py
Restart=always
RestartSec=10
StandardOutput=append:/home/ubuntu/RohaTax-App/logs/app_service.log
StandardError=append:/home/ubuntu/RohaTax-App/logs/app_service_error.log

[Install]
WantedBy=multi-user.target
```

### 5단계: 서비스 활성화 및 시작

```bash
# Systemd 리로드
sudo systemctl daemon-reload

# 서비스 활성화 (재부팅 시 자동 시작)
sudo systemctl enable rohatax.service

# 서비스 시작
sudo systemctl start rohatax.service

# 상태 확인
sudo systemctl status rohatax.service
```

### 6단계: 서비스 확인

```bash
# 서비스 상태 확인
sudo systemctl status rohatax

# 로그 확인
sudo journalctl -u rohatax -f

# 포트 확인
lsof -i :80

# HTTP 응답 확인
curl http://localhost:80
```

---

## 🎯 Systemd 서비스 관리 명령어

### 서비스 제어

```bash
# 서비스 시작
sudo systemctl start rohatax

# 서비스 중지
sudo systemctl stop rohatax

# 서비스 재시작
sudo systemctl restart rohatax

# 서비스 상태 확인
sudo systemctl status rohatax

# 서비스 활성화 (재부팅 시 자동 시작)
sudo systemctl enable rohatax

# 서비스 비활성화
sudo systemctl disable rohatax
```

### 로그 확인

```bash
# 실시간 로그 확인
sudo journalctl -u rohatax -f

# 최근 100줄 로그
sudo journalctl -u rohatax -n 100

# 오늘 로그
sudo journalctl -u rohatax --since today

# 서비스 로그 파일 확인
tail -f /home/ubuntu/RohaTax-App/logs/app_service.log
tail -f /home/ubuntu/RohaTax-App/logs/app_service_error.log
```

---

## ✅ 설정 완료 후 확인 사항

### 1. 서비스가 실행 중인지 확인

```bash
sudo systemctl status rohatax
```

**예상 결과:**
```
● rohatax.service - RohaTax Flask Application
   Loaded: loaded (/etc/systemd/system/rohatax.service; enabled; vendor preset: enabled)
   Active: active (running) since ...
```

### 2. 포트 80에서 실행 중인지 확인

```bash
lsof -i :80
```

**예상 결과:**
```
COMMAND   PID   USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
python3  1234 ubuntu    3u  IPv4  12345      0t0  TCP *:http (LISTEN)
```

### 3. HTTP 응답 확인

```bash
curl http://localhost:80
```

**예상 결과:** HTML 응답 (200 OK)

### 4. 외부에서 접속 확인

브라우저에서 `https://rohatax.com` 접속하여 정상 작동 확인

---

## 🔄 업데이트 후 재시작

### 코드 업데이트 후

```bash
# 1. Git에서 최신 코드 가져오기
cd /home/ubuntu/RohaTax-App
git pull origin main

# 2. 서비스 재시작
sudo systemctl restart rohatax

# 3. 상태 확인
sudo systemctl status rohatax
```

### 또는 배포 스크립트 사용

```bash
cd /home/ubuntu/RohaTax-App
bash scripts/server_setup.sh
# (이 스크립트는 systemd 서비스를 재시작하지 않으므로 수동으로 재시작 필요)
sudo systemctl restart rohatax
```

---

## 🎯 Systemd 서비스의 장점

### ✅ 자동 재시작
- 프로세스가 크래시되면 자동으로 재시작
- `Restart=always` 설정으로 항상 실행 유지

### ✅ 서버 재부팅 시 자동 시작
- `systemctl enable`로 활성화하면 재부팅 시 자동 시작
- 로컬 PC와 완전히 독립적으로 실행

### ✅ 로그 관리
- `journalctl`로 시스템 로그 확인 가능
- 파일 로그도 자동으로 저장

### ✅ 리소스 관리
- 시스템이 프로세스를 관리
- 메모리 누수 시 자동 감지 및 재시작

---

## 🚨 문제 해결

### 서비스가 시작되지 않으면

```bash
# 1. 로그 확인
sudo journalctl -u rohatax -n 50

# 2. 서비스 파일 확인
sudo cat /etc/systemd/system/rohatax.service

# 3. 수동 실행 테스트
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
python3 app.py
```

### 포트 80 권한 문제

```bash
# 포트 80은 root 권한 필요
# systemd 서비스는 sudo로 실행되므로 문제 없음
# 만약 문제가 있다면:
sudo setcap 'cap_net_bind_service=+ep' /home/ubuntu/RohaTax-App/venv/bin/python3
```

### .env 파일 문제

```bash
# .env 파일 확인
cat /home/ubuntu/RohaTax-App/.env

# .env 파일이 없으면 생성
cd /home/ubuntu/RohaTax-App
python3 -c "import secrets; print(secrets.token_hex(32))" > .env
echo "ENVIRONMENT=production" >> .env
echo "DEBUG=False" >> .env
```

---

## 📋 체크리스트

### 설정 완료 확인

- [ ] Systemd 서비스 파일 생성 완료
- [ ] 서비스 활성화 완료 (`systemctl enable`)
- [ ] 서비스 시작 완료 (`systemctl start`)
- [ ] 서비스 상태 확인 (`systemctl status`)
- [ ] 포트 80에서 실행 중 확인 (`lsof -i :80`)
- [ ] HTTP 응답 확인 (`curl http://localhost:80`)
- [ ] 외부 접속 확인 (`https://rohatax.com`)
- [ ] 로컬 PC 종료 후에도 서버 실행 확인

---

## 🎉 완료!

이제 인터넷 서버는:
- ✅ 로컬 PC와 완전히 독립적으로 실행
- ✅ 서버 재부팅 시 자동 시작
- ✅ 프로세스 크래시 시 자동 재시작
- ✅ 24시간 무중단 실행

**로컬 PC를 꺼도 서버는 계속 실행됩니다!** 🚀

