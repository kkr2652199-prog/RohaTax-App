# 🚨 스튜디오 상용화 보안 리스크 분석 보고서

## 📊 현재 상황 분석

### ❌ **치명적 문제점 발견**

현재 **AI 블로그 스튜디오(`/studio`)**는 **완전히 무방비 상태**입니다.

---

## 🔴 1. API 키 노출 위험

### 현재 구조
- **프론트엔드에서 직접 Gemini API 호출**
- API 키가 React 빌드 파일(`dist/assets/*.js`)에 **하드코딩**되어 노출됨
- 브라우저 개발자 도구에서 API 키를 쉽게 추출 가능

### 위험도: 🔴 **매우 높음**
- 누구나 API 키를 복사하여 무제한 사용 가능
- API 키가 유출되면 Google에서 비활성화
- 새로운 API 키 발급 필요 → 추가 비용 발생

---

## 🔴 2. 토큰 검증 부재

### 현재 구조
- **백엔드 API 엔드포인트가 없음**
- `kweon21_routes.py`는 단순히 정적 파일만 서빙
- 토큰 잔액 확인 로직이 **전혀 없음**

### 비교: 변환 기능 (안전함)
```python
# routes/conversion_modules/token_routes.py
@token_bp.route('/api/use-token', methods=['POST'])
def use_token():
    # 1. 로그인 확인
    # 2. 토큰 잔액 확인
    # 3. 토큰 차감
    # 4. 변환 작업 진행
```

### 스튜디오 (위험함)
```typescript
// kweon21/services/geminiService.ts
export const generateBlogPost = async (...) => {
    // ❌ 토큰 검증 없음
    // ❌ 백엔드 거치지 않음
    // ❌ 직접 Gemini API 호출
    const contentResponse = await ai.models.generateContent({...});
}
```

### 위험도: 🔴 **매우 높음**
- 사용자가 **무제한으로 API 호출 가능**
- 토큰 시스템과 완전히 분리됨
- 사용량 추적 불가능

---

## 🔴 3. 사용량 추적 불가능

### 현재 구조
- API 호출이 프론트엔드에서 직접 발생
- 백엔드가 중간에 개입하지 않음
- **사용량 로그가 전혀 기록되지 않음**

### 위험도: 🔴 **매우 높음**
- 누가 얼마나 사용했는지 알 수 없음
- 비용 발생 시 원인 파악 불가능
- 과금 정책 수립 불가능

---

## 💰 예상 비용 시나리오

### 시나리오 1: 악의적 사용자
- **1명이 하루에 1,000회 생성 시도**
- Gemini API 비용: 약 **$10-50/일**
- **월 비용: $300-1,500**

### 시나리오 2: 정상 사용자 (무제한)
- **100명이 하루에 10회씩 사용**
- **일일 1,000회 호출**
- **월 비용: $300-1,500**

### 시나리오 3: API 키 유출
- **유출된 키로 무제한 사용**
- **비용: 무제한 (계정 한도까지)**

---

## ✅ 해결 방안

### Phase 1: 긴급 조치 (즉시 필요)

1. **백엔드 API 엔드포인트 생성**
   - `/api/studio/generate-blog` 엔드포인트 추가
   - `/api/studio/generate-topics` 엔드포인트 추가
   - `/api/studio/generate-image` 엔드포인트 추가

2. **토큰 검증 로직 추가**
   - 각 API 호출 전 토큰 잔액 확인
   - 토큰 부족 시 요청 거부
   - 토큰 차감 및 사용량 로그 기록

3. **API 키 백엔드로 이동**
   - 프론트엔드에서 API 키 제거
   - 백엔드 `.env` 파일에서만 관리
   - React 빌드 파일에서 API 키 완전 제거

### Phase 2: 보안 강화

1. **Rate Limiting 추가**
   - 사용자별 시간당 요청 제한
   - IP 기반 차단 기능

2. **사용량 모니터링**
   - 실시간 사용량 대시보드
   - 비용 알림 시스템

3. **플랜별 제한**
   - 무료 플랜: 일일 제한
   - 유료 플랜: 월간 제한

---

## 🎯 권장 사항

### ⚠️ **상용화 전 필수 작업**

1. ✅ 백엔드 API 엔드포인트 생성
2. ✅ 토큰 검증 시스템 통합
3. ✅ API 키 백엔드로 이동
4. ✅ 사용량 로그 시스템 구축
5. ✅ Rate Limiting 구현

### 📋 **우선순위**

1. **최우선 (즉시)**: API 키 백엔드로 이동
2. **최우선 (즉시)**: 토큰 검증 로직 추가
3. **높음**: 사용량 추적 시스템
4. **중간**: Rate Limiting
5. **낮음**: 모니터링 대시보드

---

## 📝 결론

**현재 상태로는 상용화 시 심각한 비용 리스크가 있습니다.**

- ❌ API 키 노출로 인한 무제한 사용 가능
- ❌ 토큰 검증 부재로 인한 비용 발생
- ❌ 사용량 추적 불가능

**즉시 백엔드 API 엔드포인트를 생성하고 토큰 검증 시스템을 통합해야 합니다.**






