# 1단계: 배포서버 로그 확인

## 목적
실제 에러가 무엇인지 정확히 파악하기 위해 로그를 확인합니다.

---

## 배포서버에서 실행할 명령어

### 1-1. 최근 에러 로그 확인

```bash
# 최근 100줄 로그에서 API 키 관련 에러 확인
sudo journalctl -u rohatax -n 100 --no-pager | grep -i "api_key\|_get_api_key\|error\|exception"
```

**확인할 내용:**
- `_get_api_key` 함수 호출 로그
- API 키 관련 에러 메시지
- ValueError 또는 Exception 발생 여부

---

### 1-2. 전체 최근 로그 확인

```bash
# 최근 200줄 로그 전체 확인
sudo journalctl -u rohatax -n 200 --no-pager
```

**확인할 내용:**
- 서비스 시작 시간
- 최근 요청 처리 로그
- 에러나 경고 메시지

---

### 1-3. 실시간 로그 모니터링 (선택사항)

새 터미널 창을 열어서 실시간으로 로그를 확인할 수 있습니다:

```bash
# 실시간 로그 확인 (Ctrl+C로 종료)
sudo journalctl -u rohatax -f
```

---

## 예상되는 로그 패턴

### 정상 작동 시:
```
[_get_api_key] 사용자 API 키 조회 시작 (user_id=2)
[_get_api_key] 사용자 API 키 발견 (user_id=2, 길이=39)
```

### 문제 발생 시:
```
[_get_api_key] 사용자 API 키 조회 시작 (user_id=2)
[_get_api_key] 사용자 API 키가 NULL (user_id=2)
[_get_api_key] 서버 환경변수에서 API 키 조회 시도
[_get_api_key] 서버 환경변수에서 API 키 사용 (길이=39)
```

또는:
```
[_get_api_key] API 키가 설정되지 않았습니다...
ValueError: API 키가 설정되지 않았습니다...
```

---

## 다음 단계

로그 확인 결과를 공유해주시면:
- 정확한 문제점 파악
- 2단계 진단 스크립트 실행 방법 안내
- 필요한 수정 사항 제시

**로그 결과를 복사해서 공유해주세요!**


