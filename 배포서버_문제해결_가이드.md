# 🚨 배포 서버 문제 해결 가이드

## 📋 빠른 진단 및 복구

### 1단계: 진단 실행

배포 서버 터미널에서 다음 명령어를 실행하세요:

```bash
cd /home/ubuntu/RohaTax-App
chmod +x 배포서버_진단_복구.sh
bash 배포서버_진단_복구.sh
```

이 스크립트는 다음을 확인합니다:
- ✅ 현재 실행 중인 프로세스 상태
- ✅ 최근 로그 파일 내용
- ✅ 환경 변수 설정
- ✅ 가상환경 상태
- ✅ Python 의존성
- ✅ 코드 문법 오류
- ✅ 데이터베이스 파일
- ✅ .env 파일
- ✅ Pydantic 경고 (orm_mode)

### 2단계: 자동 복구 실행

진단 결과를 확인한 후, 자동 복구를 실행하세요:

```bash
chmod +x 배포서버_복구_실행.sh
bash 배포서버_복구_실행.sh
```

이 스크립트는 다음을 수행합니다:
1. 기존 프로세스 완전 종료
2. 가상환경 확인 및 활성화
3. 의존성 업데이트
4. 환경 변수 설정
5. 로그 디렉토리 준비
6. 코드 문법 검사
7. 데이터베이스 확인
8. Flask 서버 시작 (screen 세션)
9. 실행 확인

---

## 🔍 주요 문제 및 해결 방법

### 문제 1: Pydantic UserWarning (orm_mode)

**증상:**
```
UserWarning: 'orm_mode' has been renamed to 'from_attributes' in V2
```

**원인:**
- Pydantic V2에서 `orm_mode`가 `from_attributes`로 변경됨
- 코드에서 아직 구버전 문법 사용

**해결:**
✅ 이미 수정 완료: `homepage1/core/payment/schemas.py`에서 `orm_mode` → `from_attributes`로 변경

**확인:**
```bash
grep -r "orm_mode" --include="*.py" .
```

### 문제 2: Flask 서버 시작 실패

**증상:**
- "FAILED check log above" 메시지
- 포트 5000에서 서버가 실행되지 않음

**해결 방법:**

1. **기존 프로세스 완전 종료:**
```bash
lsof -ti:5000 | xargs kill -9
pkill -9 -f "python3.*app.py"
screen -S rohatax -X quit
```

2. **로그 확인:**
```bash
tail -50 /home/ubuntu/flask_server.log
```

3. **수동 실행 (디버깅용):**
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
export FLASK_RUN_HOST=0.0.0.0
export FLASK_RUN_PORT=5000
export FLASK_ENV=production
python3 app.py
```

### 문제 3: 의존성 오류

**증상:**
- ImportError 발생
- 패키지가 설치되지 않음

**해결:**
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 문제 4: 데이터베이스 오류

**증상:**
- 데이터베이스 연결 실패
- 테이블이 없다는 오류

**해결:**
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
python3 -c "from core.db import init_db; init_db()"
```

### 문제 5: 환경 변수 누락

**증상:**
- SECRET_KEY 오류
- 환경 변수 관련 오류

**해결:**
```bash
cd /home/ubuntu/RohaTax-App
# .env 파일 확인
cat .env

# 필요한 환경 변수 설정
export SECRET_KEY="your-secret-key"
export FLASK_ENV=production
```

---

## 📊 서버 상태 확인 명령어

### 실행 중인 프로세스 확인
```bash
lsof -i :5000
ps aux | grep "python3.*app.py"
```

### Screen 세션 확인
```bash
screen -ls
screen -r rohatax  # 세션 접속
# 나가기: Ctrl+A, D
```

### 로그 확인
```bash
# 실시간 로그 확인
tail -f /home/ubuntu/flask_server.log

# 최근 50줄
tail -50 /home/ubuntu/flask_server.log

# 오류만 필터링
tail -100 /home/ubuntu/flask_server.log | grep -i error
```

### 서버 헬스 체크
```bash
curl http://localhost:5000/health
```

---

## 🔄 정상적인 서버 재시작 절차

1. **기존 프로세스 종료:**
```bash
screen -S rohatax -X quit
lsof -ti:5000 | xargs kill -9 2>/dev/null || true
```

2. **코드 업데이트 (필요시):**
```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
```

3. **서버 시작:**
```bash
bash 배포서버_복구_실행.sh
```

또는 수동으로:
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
export FLASK_RUN_HOST=0.0.0.0
export FLASK_RUN_PORT=5000
export FLASK_ENV=production
screen -dmS rohatax bash -c "cd /home/ubuntu/RohaTax-App && source venv/bin/activate && export FLASK_RUN_HOST=0.0.0.0 && export FLASK_RUN_PORT=5000 && python3 app.py 2>&1 | tee /home/ubuntu/flask_server.log"
```

4. **확인:**
```bash
sleep 5
lsof -i :5000
tail -20 /home/ubuntu/flask_server.log
```

---

## 📝 체크리스트

배포 전 확인:
- [ ] Git에서 최신 코드 가져오기 (`git pull`)
- [ ] 진단 스크립트 실행 (`bash 배포서버_진단_복구.sh`)
- [ ] 오류 확인 및 수정
- [ ] 복구 스크립트 실행 (`bash 배포서버_복구_실행.sh`)
- [ ] 서버 로그 확인 (`tail -f /home/ubuntu/flask_server.log`)
- [ ] 브라우저에서 접속 테스트 (`https://rohatax.com`)

---

## 🆘 긴급 복구

모든 것이 실패했을 때:

```bash
cd /home/ubuntu/RohaTax-App
git checkout 04d2c05  # 안정적인 커밋으로 롤백
sudo pkill -9 python3
source venv/bin/activate
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > /tmp/flask.log 2>&1 &
sleep 5
tail -30 /tmp/flask.log
lsof -i :5000
```

---

## 📞 추가 도움

문제가 지속되면:
1. 진단 스크립트의 전체 출력을 확인
2. 로그 파일의 오류 메시지 확인
3. 환경 변수 및 설정 파일 확인
4. 데이터베이스 상태 확인

