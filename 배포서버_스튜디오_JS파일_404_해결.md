# 배포 서버 스튜디오 JS 파일 404 오류 해결

**문제**: `/studio/app/assets/index-DofkCTji.js` 파일이 404 오류  
**원인**: 배포 서버의 빌드 파일이 로컬과 다름

---

## 🔍 발견된 문제

### 브라우저 콘솔 오류
1. ✅ **X-Frame-Options 오류 해결됨** (더 이상 없음)
2. ❌ **JS 파일 404 오류**: `https://www.rohatax.com/studio/app/assets/index-DofkCTji.js` - 404
3. ⚠️ **Content Security Policy 오류**: 외부 리소스 차단

### 네트워크 요청 분석
- ✅ `/studio/app` - 200 OK (iframe 로드 성공)
- ✅ `/studio/app/assets/index-CgcUtD30.css` - 200 OK
- ❌ `/studio/app/assets/index-DofkCTji.js` - 404 Not Found

---

## ✅ 해결 방법

### 문제 1: JS 파일 404 오류

**원인**: 배포 서버의 빌드 파일이 로컬과 다름

**해결**: 로컬에서 최신 빌드 후 배포 서버에 업로드

#### 로컬에서 빌드

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. kweon21 폴더로 이동
cd kweon21

# 3. 최신 빌드 실행
npm run build

# 4. 빌드 결과 확인
dir dist\assets
```

**확인 사항:**
- `index-DofkCTji.js` 파일이 있는가?
- `index-CgcUtD30.css` 파일이 있는가?

#### Git에 추가 및 푸시

```bash
# 1. RohaTax 폴더로 이동
cd C:\Users\user\Desktop\RohaTax

# 2. 빌드 파일 추가
git add kweon21/dist/

# 3. 커밋
git commit -m "Update kweon21 dist build files with latest JS assets"

# 4. 하늘저장소에 푸시
git push origin main
```

#### 배포 서버에서 가져오기

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. 하늘저장소에서 최신 코드 가져오기
git pull origin main

# 3. JS 파일 확인
ls -lh kweon21/dist/assets/index-DofkCTji.js

# 4. 서비스 재시작
sudo systemctl restart rohatax
```

---

### 문제 2: Content Security Policy 오류

**해결**: CSP 설정에 외부 리소스 도메인 추가 (이미 수정 완료)

수정된 CSP:
- `script-src`: `https://cdn.tailwindcss.com`, `https://esm.sh`, `https://static.cloudflareinsights.com` 추가
- `style-src`: `https://fonts.googleapis.com`, `https://cdn.jsdelivr.net` 추가
- `font-src`: `https://cdn.jsdelivr.net`, `https://fonts.gstatic.com` 추가
- `img-src`: `https://cdn.jsdelivr.net` 추가

---

## 📋 배포 서버에서 확인

### JS 파일 존재 확인

```bash
# 배포 서버 터미널에서
ls -lh /home/ubuntu/RohaTax-App/kweon21/dist/assets/
```

**확인 사항:**
- `index-DofkCTji.js` 파일이 있는가?
- 파일 크기가 0이 아닌가?

---

## ✅ 완료 체크리스트

- [x] CSP 설정 수정 완료
- [ ] 로컬에서 최신 빌드 실행
- [ ] Git에 빌드 파일 추가 및 푸시
- [ ] 배포 서버에서 `git pull` 실행
- [ ] 배포 서버에서 JS 파일 확인
- [ ] 서비스 재시작
- [ ] 브라우저에서 정상 작동 확인

---

## 🎯 다음 단계

1. **로컬에서 빌드 실행**
2. **Git에 추가 및 푸시**
3. **배포 서버에서 가져오기**
4. **브라우저에서 확인**

---

**가장 중요한 것은 JS 파일이 배포 서버에 있는지 확인하는 것입니다!**

