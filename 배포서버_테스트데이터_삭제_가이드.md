# 배포 서버 테스트 데이터 삭제 가이드

**목적**: 배포 서버의 테스트 데이터만 안전하게 삭제  
**보존**: 유저 정보, 기능은 절대 건들지 않음

---

## 🎯 삭제 대상

### ✅ 삭제할 것 (테스트 데이터)
1. **결제 로그** (payment_history)
2. **활동 로그** (activity_logs) - 로그인/로그아웃 등
3. **황동 구매 내역** (token_history에서 구매 관련)
4. **변환 로그** (conversion_logs)

### ❌ 절대 건들지 않을 것
1. **유저 정보** (users 테이블)
   - 사용자 계정
   - 이메일
   - 등급 (plan_type)
   - 토큰 잔액 (token_balance)
2. **기능**
   - 모든 기능은 그대로 유지

---

## 📋 실행 방법

### 배포 서버 터미널에서

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App
```

```bash
# 2. 하늘저장소에서 최신 스크립트 가져오기
git pull origin main
```

```bash
# 3. 스크립트 실행 권한 부여
chmod +x scripts/clear_test_data_safe.py
```

```bash
# 4. 스크립트 실행
python3 scripts/clear_test_data_safe.py
```

---

## ⚠️ 안전장치

### 자동 백업
- 스크립트 실행 전 **자동으로 데이터베이스 백업** 생성
- 백업 위치: `database/backups/app_db_before_clear_test_YYYYMMDD_HHMMSS.db`

### 확인 절차
- 실행 전 확인 메시지 표시
- 삭제 전/후 레코드 수 표시
- 유저 정보 확인

---

## 📊 실행 결과 예시

```
============================================================
배포 서버 테스트 데이터 삭제 스크립트
============================================================

⚠️  주의사항:
  - 유저 정보는 절대 건들지 않습니다
  - 기능은 그대로 유지됩니다
  - 테스트 데이터만 삭제됩니다

계속하시겠습니까? (yes/no): yes

📦 데이터베이스 백업 생성 중...
✅ 백업 완료

[삭제 전 레코드 수]
  users: 10개
  payment_history: 50개
  activity_logs: 200개
  token_history: 30개
  conversion_logs: 100개

[1] 결제 로그 삭제 중...
  ✅ 50개 결제 로그 삭제됨

[2] 활동 로그 삭제 중...
  ✅ 200개 활동 로그 삭제됨

[3] 황동 구매 내역 삭제 중...
  ✅ 30개 구매 내역 삭제됨

[4] 변환 로그 삭제 중...
  ✅ 100개 변환 로그 삭제됨

[삭제 후 레코드 수]
  users: 10개 (변화 없음 ✅)
  payment_history: 0개
  activity_logs: 0개
  token_history: 0개
  conversion_logs: 0개

✅ 유저 정보 확인 (절대 건들지 않음)
총 유저 수: 10명
```

---

## ✅ 완료 확인

### 유저 정보 확인
```bash
# 배포 서버에서
sqlite3 database/app.db "SELECT COUNT(*) FROM users;"
sqlite3 database/app.db "SELECT id, username, email FROM users LIMIT 5;"
```

### 삭제 확인
```bash
# 결제 로그 확인 (0개여야 함)
sqlite3 database/app.db "SELECT COUNT(*) FROM payment_history;"

# 활동 로그 확인 (0개여야 함)
sqlite3 database/app.db "SELECT COUNT(*) FROM activity_logs;"
```

---

## 🆘 문제 발생 시

### 백업에서 복원
```bash
# 백업 파일 확인
ls -lh database/backups/app_db_before_clear_test_*.db

# 백업에서 복원
cp database/backups/app_db_before_clear_test_YYYYMMDD_HHMMSS.db database/app.db
chown ubuntu:ubuntu database/app.db
chmod 644 database/app.db

# 서비스 재시작
sudo systemctl restart rohatax
```

---

## 📝 요약

1. **스크립트 실행**: `python3 scripts/clear_test_data_safe.py`
2. **자동 백업**: 실행 전 자동으로 백업 생성
3. **안전 삭제**: 테스트 데이터만 삭제, 유저 정보 보존
4. **확인**: 유저 정보가 그대로인지 확인

**유저 정보는 절대 건들지 않습니다!**

