# 배포 서버 진단 결과 분석

**진단 시간**: 2025-01-01 06:55 (서버 시간 기준)  
**상태**: 현재 CPU 사용량 정상, 간헐적 문제 가능성

---

## ✅ 현재 상태 (정상)

### 1. CPU 사용량
- **Load Average**: 0.00, 0.05, 0.07 (1분, 5분, 15분)
- **CPU 상태**: 거의 유휴 상태 (99% 이상 idle)
- **Gunicorn 프로세스**: 0.3-0.4% CPU 사용 (정상)

### 2. 메모리 사용량
- **사용 중**: 217M / 417M (정상 범위)

### 3. Gunicorn 프로세스
- **상태**: 정상 실행 중
- **워커 수**: 4개 (`-w 4`)
- **포트**: 80번 포트에서 리스닝
- **CPU 사용량**: 각각 0.3-0.4% (매우 낮음)

### 4. 데이터베이스
- **파일 크기**: 560K
- **수정 시간**: 2025-01-01 06:38

---

## ⚠️ 발견된 문제

### 1. `netstat` 명령어 없음
- **문제**: `netstat: command not found`
- **영향**: 네트워크 연결 수 확인 불가
- **해결**: `ss` 명령어로 대체 (스크립트 수정 완료)

### 2. 출력 인코딩 문제
- **문제**: 일부 출력이 깨져서 보임
- **영향**: 일부 정보 확인 어려움
- **해결**: Python 스크립트 사용 권장

---

## 🔍 CPU 급상승 원인 분석

### 현재 상태
- **CPU 사용량**: 거의 0% (정상)
- **Load Average**: 매우 낮음 (0.00-0.07)

### 가능한 원인
1. **간헐적 문제**
   - 특정 요청 시에만 발생
   - 특정 시간대에만 발생
   - 특정 사용자 액션 시에만 발생

2. **API 키 관련 무한 루프**
   - 사용자가 `/studio` 페이지 접속 시
   - API 키가 없어서 계속 재시도
   - → 수정된 코드가 아직 배포 서버에 반영되지 않음

3. **데이터베이스 잠금**
   - 동시 접속이 많을 때
   - 특정 쿼리가 느릴 때

4. **메모리 부족**
   - Swap 사용 중 (274M / 1024M)
   - 메모리 부족 시 CPU 사용량 증가 가능

---

## 📋 다음 단계

### 1단계: 수정된 스크립트로 재진단

배포 서버에서:

```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
bash scripts/deploy_server_cpu_diagnosis.sh
```

또는 Python 스크립트 사용:

```bash
python3 scripts/deploy_server_cpu_diagnosis.py
```

### 2단계: CPU 급상승 발생 시점 모니터링

CPU 급상승이 발생할 때:

```bash
# 실시간 모니터링
htop

# 또는
top
```

**확인 사항:**
- 어떤 프로세스가 CPU를 많이 사용하는가?
- Gunicorn 워커 중 특정 워커가 문제인가?
- 메모리 사용량은?

### 3단계: 서비스 로그 확인

```bash
# 최근 로그 확인 (에러 위주)
sudo journalctl -u rohatax -n 100 --no-pager | grep -i -E "error|exception|traceback|failed|critical"

# 실시간 로그 모니터링
sudo journalctl -u rohatax -f
```

**확인 사항:**
- API 키 관련 에러가 반복되는가?
- 특정 엔드포인트에서 에러가 발생하는가?
- 데이터베이스 관련 에러가 있는가?

### 4단계: Google API 키 수정 적용 확인

```bash
# 최신 코드 확인
cd /home/ubuntu/RohaTax-App
git log --oneline -5

# 서비스 재시작 (최신 코드 반영)
sudo systemctl restart rohatax
sudo systemctl status rohatax
```

**확인 사항:**
- Google API 키 수정 코드가 반영되었는가?
- 서비스가 정상적으로 재시작되었는가?

---

## 🎯 권장 조치 사항

### 즉시 조치
1. **최신 코드 반영 확인**
   ```bash
   cd /home/ubuntu/RohaTax-App
   git pull origin main
   sudo systemctl restart rohatax
   ```

2. **수정된 진단 스크립트로 재진단**
   ```bash
   bash scripts/deploy_server_cpu_diagnosis.sh
   ```

### CPU 급상승 발생 시
1. **실시간 모니터링**
   ```bash
   htop
   ```

2. **로그 확인**
   ```bash
   sudo journalctl -u rohatax -f
   ```

3. **프로세스 확인**
   ```bash
   ps aux --sort=-%cpu | head -20
   ```

---

## 📊 예상 시나리오

### 시나리오 1: API 키 관련 무한 루프
- **증상**: `/studio` 페이지 접속 시 CPU 급상승
- **원인**: 사용자별 API 키가 없어서 계속 재시도
- **해결**: Google API 키 수정 코드 반영 후 재시작

### 시나리오 2: 데이터베이스 잠금
- **증상**: 동시 접속이 많을 때 CPU 급상승
- **원인**: 데이터베이스 파일 잠금
- **해결**: 데이터베이스 연결 풀 최적화

### 시나리오 3: 메모리 부족
- **증상**: Swap 사용량 증가와 함께 CPU 급상승
- **원인**: 메모리 부족으로 인한 스왑 사용
- **해결**: 메모리 사용량 최적화 또는 서버 업그레이드

---

## ✅ 체크리스트

- [x] 진단 스크립트 실행 완료
- [x] 현재 CPU 사용량 정상 확인
- [ ] 수정된 스크립트로 재진단
- [ ] 최신 코드 반영 확인
- [ ] CPU 급상승 발생 시점 모니터링
- [ ] 서비스 로그 확인

---

**다음 단계: 수정된 스크립트로 재진단하고, CPU 급상승 발생 시점을 모니터링하세요!**




