# 🚀 배포 서버 완전 초기화 가이드

> **대상**: 초보 개발자를 위한 상세 가이드  
> **목적**: 배포 서버를 깨끗한 상태로 초기화하고 처음부터 다시 설정

---

## 📋 초기화 전 확인 사항

### ⚠️ 중요: 데이터 백업

초기화하면 **모든 데이터가 삭제**됩니다:
- ❌ 회원 정보 (users 테이블)
- ❌ 결제 내역 (payment_history)
- ❌ 토큰 이력 (token_history)
- ❌ 모든 사용자 데이터

**백업이 필요한 경우:**
- 실제 사용 중인 데이터가 있는 경우
- 회원 정보를 보존해야 하는 경우

**백업이 필요 없는 경우:**
- 테스트 데이터만 있는 경우
- 개발/테스트 서버인 경우

---

## 🎯 방법 선택

### 방법 1: 데이터 백업 후 초기화 (권장)
- ✅ 기존 데이터 보존
- ✅ 안전한 초기화
- ⏱️ 시간: 약 10-15분

### 방법 2: 데이터 백업 없이 초기화 (빠름)
- ✅ 빠른 초기화
- ❌ 모든 데이터 삭제
- ⏱️ 시간: 약 5-10분

---

## 📦 방법 1: 데이터 백업 후 초기화 (상세 가이드)

### 1단계: 데이터베이스 백업

배포 서버 터미널에서 실행:

```bash
cd /home/ubuntu/RohaTax-App
echo "=== 1단계: 데이터베이스 백업 ==="
mkdir -p database/backups
cp database/app.db database/backups/app_backup_$(date +%Y%m%d_%H%M%S).db
echo "✅ 백업 완료: database/backups/app_backup_*.db"
ls -lh database/backups/
```

**이 명령어가 하는 일:**
- `database/backups/` 폴더 생성
- 현재 데이터베이스를 백업 파일로 복사
- 백업 파일명에 날짜/시간 포함

### 2단계: 서버 프로세스 종료

```bash
echo "=== 2단계: 서버 프로세스 종료 ==="
lsof -ti:5000 | xargs kill -9 2>/dev/null || true
pkill -9 -f "python3.*app.py" 2>/dev/null || true
screen -S rohatax -X quit 2>/dev/null || true
sleep 2
echo "✅ 모든 프로세스 종료 완료"
```

### 3단계: 기존 데이터베이스 삭제

```bash
echo "=== 3단계: 기존 데이터베이스 삭제 ==="
cd /home/ubuntu/RohaTax-App
rm -f database/app.db
rm -f database/app.db-journal
rm -f database/app.db-wal
rm -f database/app.db-shm
echo "✅ 기존 데이터베이스 삭제 완료"
```

**주의:** 백업 파일은 삭제되지 않습니다!

### 4단계: Git 상태 확인 및 정리

```bash
echo "=== 4단계: Git 상태 확인 ==="
cd /home/ubuntu/RohaTax-App
git status
echo ""
echo "변경사항이 있으면 임시 저장합니다..."
git stash
echo "✅ Git 상태 정리 완료"
```

### 5단계: 최신 코드 가져오기

```bash
echo "=== 5단계: 최신 코드 가져오기 ==="
cd /home/ubuntu/RohaTax-App
git fetch origin
git pull origin main
echo "✅ 최신 코드 가져오기 완료"
```

### 6단계: 가상환경 확인 및 의존성 설치

```bash
echo "=== 6단계: 가상환경 및 의존성 확인 ==="
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
echo "✅ 의존성 설치 완료"
```

### 7단계: 새 데이터베이스 생성

```bash
echo "=== 7단계: 새 데이터베이스 생성 ==="
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
python3 -c "from core.db import init_db, seed_demo; init_db(); seed_demo(); print('✅ 데이터베이스 초기화 완료')"
echo "✅ 새 데이터베이스 생성 완료"
```

### 8단계: 서버 시작

```bash
echo "=== 8단계: 서버 시작 ==="
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
export FLASK_RUN_HOST=0.0.0.0
export FLASK_RUN_PORT=5000
export FLASK_ENV=production
nohup python3 app.py > /home/ubuntu/flask_server.log 2>&1 &
sleep 5
echo "✅ 서버 시작 완료"
```

### 9단계: 서버 확인

```bash
echo "=== 9단계: 서버 확인 ==="
lsof -i :5000
echo ""
curl -s http://localhost:5000/health
echo ""
echo "✅ 서버 상태 확인 완료"
```

---

## ⚡ 방법 2: 빠른 초기화 (데이터 백업 없음)

**주의:** 모든 데이터가 삭제됩니다!

배포 서버 터미널에서 한 번에 실행:

```bash
cd /home/ubuntu/RohaTax-App && \
echo "=== 빠른 초기화 시작 ===" && \
lsof -ti:5000 | xargs kill -9 2>/dev/null || true && \
pkill -9 -f "python3.*app.py" 2>/dev/null || true && \
screen -S rohatax -X quit 2>/dev/null || true && \
sleep 2 && \
rm -f database/app.db database/app.db-* && \
git stash && \
git pull origin main && \
source venv/bin/activate && \
pip install -r requirements.txt && \
python3 -c "from core.db import init_db, seed_demo; init_db(); seed_demo()" && \
export FLASK_RUN_HOST=0.0.0.0 && \
export FLASK_RUN_PORT=5000 && \
export FLASK_ENV=production && \
nohup python3 app.py > /home/ubuntu/flask_server.log 2>&1 & \
sleep 5 && \
lsof -i :5000 && \
curl -s http://localhost:5000/health && \
echo "" && \
echo "✅ 초기화 완료!"
```

---

## 🔍 초기화 후 확인 사항

### 1. 서버 실행 확인
```bash
lsof -i :5000
ps aux | grep "python3.*app.py" | grep -v grep
```

### 2. 헬스 체크
```bash
curl http://localhost:5000/health
```

### 3. 브라우저에서 확인
- 메인 페이지: `https://rohatax.com`
- 관리자 대시보드: `https://rohatax.com/admin`
- 로그인 테스트

### 4. 기본 관리자 계정
초기화 후 기본 관리자 계정:
- **아이디**: `kweon4309`
- **비밀번호**: `1234`

---

## 🆘 문제 해결

### 문제 1: "Permission denied" 오류
```bash
cd /home/ubuntu/RohaTax-App
sudo chown -R ubuntu:ubuntu .
sudo chmod -R 755 .
```

### 문제 2: 데이터베이스 파일 권한 오류
```bash
cd /home/ubuntu/RohaTax-App
sudo chown -R ubuntu:ubuntu database/
chmod 644 database/app.db
```

### 문제 3: 서버가 시작되지 않음
```bash
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
python3 app.py
# 오류 메시지를 확인하세요
```

---

## 📝 초기화 완료 체크리스트

- [ ] 서버가 포트 5000에서 실행 중
- [ ] 헬스 체크 성공 (`{"status":"ok"}`)
- [ ] 메인 페이지 접속 가능
- [ ] 관리자 대시보드 접속 가능
- [ ] 로그인 테스트 성공
- [ ] 데이터베이스 파일 생성 확인 (`database/app.db`)

---

## 💡 추천 방법

**초보 개발자에게 추천:**
- **방법 1 (백업 후 초기화)**를 권장합니다
- 단계별로 실행하면서 각 단계의 결과를 확인하세요
- 문제가 발생하면 해당 단계에서 멈추고 확인하세요

**빠르게 진행하려면:**
- **방법 2 (빠른 초기화)**를 사용하세요
- 하지만 데이터 손실에 주의하세요

---

## 🎯 다음 단계

초기화 완료 후:
1. 로컬에서 개발한 최신 코드를 본진으로 동기화
2. 본진에서 하늘저장소로 푸시
3. 배포 서버에서 `git pull` 후 재시작

이제 깨끗한 상태에서 시작할 수 있습니다!

