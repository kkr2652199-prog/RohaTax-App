-- File: database/migrations/004_add_previous_plan_type.sql

-- Description: 결제 취소 시 등급 원상복구를 위한 이전 등급 저장 컬럼 추가
-- 결제 생성 시 사용자의 이전 등급을 저장하여, 결제 취소 시 원래 등급으로 복구 가능하도록 함

-- ====================================================================
-- 마이그레이션: payment_history 테이블에 previous_plan_type 컬럼 추가
-- ====================================================================

ALTER TABLE payment_history 
ADD COLUMN previous_plan_type TEXT;

-- ====================================================================
-- 인덱스 생성 (선택사항, 등급 복구 조회 시 성능 최적화)
-- ====================================================================
-- previous_plan_type은 NULL이 많을 수 있으므로 인덱스는 선택사항
-- CREATE INDEX IF NOT EXISTS idx_payment_history_previous_plan_type ON payment_history(previous_plan_type);

-- ====================================================================
-- 마이그레이션 완료
-- ====================================================================

