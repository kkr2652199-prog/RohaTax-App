# 🔐 로그인 문제 해결 가이드

## 📋 현재 상황
- ✅ 웹사이트 접속 성공 (`https://rohatax.com`)
- ❌ 로그인 기능 작동 안 함

---

## 🔍 문제 진단 (단계별 확인)

### 1단계: 서버 로그 확인

**Lightsail 터미널에서 실행:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
tail -50 app.log
```

**확인 사항:**
- 로그인 시도 시 오류 메시지가 있는지
- 데이터베이스 연결 오류가 있는지
- SECRET_KEY 관련 경고가 있는지

---

### 2단계: 데이터베이스 확인

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
ls -la database/
```

**예상 결과:** `app.db` 파일이 있어야 합니다.

**데이터베이스가 없으면:**

<--- 복사하세요
```
python3 -c "from core.db import init_db; init_db()"
```

---

### 3단계: 사용자 계정 확인

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT id, username, email, is_active, approval_status FROM users LIMIT 5'); print('\n'.join([str(row) for row in cursor.fetchall()])); conn.close()"
```

**확인 사항:**
- 사용자 계정이 있는지
- `is_active = 1`인지
- `approval_status = 'approved'`인지

---

### 4단계: SECRET_KEY 확인

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
cat .env | grep SECRET_KEY
```

**또는:**

<--- 복사하세요
```
python3 -c "from config.settings import settings; print('SECRET_KEY:', '설정됨' if settings.SECRET_KEY else '미설정')"
```

**문제:** SECRET_KEY가 없으면 세션이 제대로 작동하지 않습니다.

**해결 방법:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
```

출력된 SECRET_KEY를 `.env` 파일에 추가:

<--- 복사하세요
```
nano .env
```

`.env` 파일에 다음 추가:
```
SECRET_KEY=생성된-키-여기에-붙여넣기
```

저장: `Ctrl + X`, `Y`, `Enter`

---

### 5단계: 테스트 사용자 계정 생성

**사용자 계정이 없으면:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 << 'EOF'
import sqlite3
from core.password_utils import hash_password

conn = sqlite3.connect('database/app.db')
cursor = conn.cursor()

# 테스트 사용자 생성
username = 'test'
password = 'test1234'
hashed_password = hash_password(password)

try:
    cursor.execute('''
        INSERT INTO users (username, password, email, is_active, approval_status, plan_type)
        VALUES (?, ?, ?, 1, 'approved', 'free')
    ''', (username, hashed_password, f'{username}@test.com'))
    conn.commit()
    print(f'✅ 테스트 사용자 생성 완료: {username} / {password}')
except sqlite3.IntegrityError:
    print('⚠️ 사용자 이미 존재합니다.')
finally:
    conn.close()
EOF
```

---

### 6단계: 서버 재시작

**SECRET_KEY를 변경했거나 데이터베이스를 초기화했다면:**

<--- 복사하세요
```
pkill -f "python3 app.py"
```

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 nohup python3 app.py > app.log 2>&1 &
```

<--- 복사하세요
```
sleep 3 && tail -20 app.log
```

---

## 🎯 일반적인 문제와 해결책

### 문제 1: "비밀번호가 올바르지 않습니다"
**원인:** 
- 사용자 계정이 없음
- 비밀번호 해시가 잘못됨

**해결:**
- 위의 "5단계: 테스트 사용자 계정 생성" 실행

### 문제 2: "승인 대기/거부된 계정입니다"
**원인:** 
- `approval_status`가 'approved'가 아님

**해결:**

<--- 복사하세요
```
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); conn.execute(\"UPDATE users SET approval_status = 'approved' WHERE username = 'test'\"); conn.commit(); conn.close(); print('✅ 승인 상태 업데이트 완료')"
```

### 문제 3: 세션이 유지되지 않음
**원인:** 
- SECRET_KEY가 설정되지 않음
- HTTPS에서 SECRET_KEY가 변경됨

**해결:**
- 위의 "4단계: SECRET_KEY 확인" 실행

### 문제 4: 데이터베이스 연결 오류
**원인:** 
- 데이터베이스 파일이 없음
- 권한 문제

**해결:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
mkdir -p database
chmod 755 database
python3 -c "from core.db import init_db; init_db()"
```

---

## 📝 체크리스트

로그인 문제 해결 전 확인:
- [ ] 서버 로그 확인 (오류 메시지 확인)
- [ ] 데이터베이스 파일 존재 확인
- [ ] 사용자 계정 존재 확인
- [ ] SECRET_KEY 설정 확인
- [ ] 서버 재시작 (설정 변경 후)

---

## 🆘 여전히 안 되면

1. **브라우저 개발자 도구 확인**
   - F12 → Console 탭
   - 로그인 시도 시 오류 메시지 확인

2. **서버 로그 실시간 확인**

<--- 복사하세요
```
tail -f app.log
```

브라우저에서 로그인 시도하면서 로그 확인

3. **데이터베이스 직접 확인**

<--- 복사하세요
```
sqlite3 database/app.db "SELECT * FROM users;"
```

---

## ✅ 성공 확인

로그인 성공 시:
- 대시보드 페이지로 리다이렉트
- 세션이 유지됨 (페이지 새로고침해도 로그인 상태 유지)

