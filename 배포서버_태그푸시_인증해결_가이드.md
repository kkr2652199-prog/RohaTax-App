# 배포 서버 태그 푸시 인증 문제 해결 가이드

**문제**: `git push origin --tags` 실행 시 인증 실패
**오류 메시지**: "Password authentication is not supported for Git operations"

---

## 🔍 문제 원인

GitHub는 2021년부터 비밀번호 인증을 중단했습니다. 
**Personal Access Token** 또는 **SSH 키**를 사용해야 합니다.

---

## ✅ 해결 방법 (3가지 중 선택)

### 방법 1: Personal Access Token을 URL에 포함 (가장 간단)

**배포 서버 터미널에서 실행:**

```bash
# 1. 현재 원격 저장소 URL 확인
git remote -v
```

```bash
# 2. Personal Access Token을 URL에 포함하여 푸시
# 형식: https://[토큰]@github.com/[사용자명]/[저장소명].git
git push https://[여기에_토큰_입력]@github.com/kkr2652199-prog/RohaTax-App.git --tags
```

**예시:**
```bash
git push https://ghp_xxxxxxxxxxxxxxxxxxxx@github.com/kkr2652199-prog/RohaTax-App.git --tags
```

**장점**: 한 번만 실행하면 됨
**단점**: 명령어에 토큰이 노출됨 (히스토리에 남을 수 있음)

---

### 방법 2: 원격 저장소 URL을 토큰 포함으로 변경 (추천)

**배포 서버 터미널에서 실행:**

```bash
# 1. Personal Access Token 준비 (로컬 컴퓨터에서 GitHub에서 생성)

# 2. 원격 저장소 URL 변경
git remote set-url origin https://[토큰]@github.com/kkr2652199-prog/RohaTax-App.git

# 예시:
# git remote set-url origin https://ghp_xxxxxxxxxxxxxxxxxxxx@github.com/kkr2652199-prog/RohaTax-App.git
```

```bash
# 3. 변경 확인
git remote -v
```

```bash
# 4. 태그 푸시 (이제 인증 없이 가능)
git push origin --tags
```

**장점**: 이후 푸시 시 인증 불필요
**주의**: 토큰이 저장되므로 보안 주의

---

### 방법 3: 로컬 컴퓨터에서 푸시 (가장 안전)

**배포 서버에서 태그를 로컬로 가져와서 로컬에서 푸시**

**배포 서버 터미널:**
```bash
# 태그는 이미 생성되어 있으므로 아무것도 안 해도 됨
```

**로컬 컴퓨터 (본진) 터미널:**
```bash
# 1. 하늘저장소에서 태그 가져오기
cd C:\Users\user\Desktop\RohaTax
git fetch origin --tags

# 2. 태그 확인
git tag -l

# 3. 태그 푸시 (로컬에서는 이미 인증되어 있을 것)
git push origin --tags
```

**장점**: 가장 안전, 토큰 노출 없음
**단점**: 배포 서버에서 직접 푸시 불가

---

## 📋 Personal Access Token 생성 방법 (로컬 컴퓨터에서)

1. **GitHub 웹사이트 접속**
   - https://github.com 로그인

2. **Settings 이동**
   - 우측 상단 프로필 아이콘 클릭
   - Settings 클릭

3. **Developer settings 이동**
   - 좌측 하단 "Developer settings" 클릭

4. **Personal access tokens 생성**
   - "Personal access tokens" → "Tokens (classic)" 클릭
   - "Generate new token" → "Generate new token (classic)" 클릭

5. **토큰 설정**
   - Note: "배포서버 태그 푸시" 입력
   - Expiration: 원하는 기간 선택 (예: 90 days)
   - 권한: **`repo`** 체크박스 선택
   - 하단 "Generate token" 클릭

6. **토큰 복사**
   - 생성된 토큰 복사 (한 번만 표시됨!)
   - 형식: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## 🎯 권장 방법: 방법 2 (원격 저장소 URL 변경)

**배포 서버 터미널에서 순서대로 실행:**

```bash
# 1단계: 원격 저장소 URL 확인
git remote -v
```

```bash
# 2단계: Personal Access Token을 포함한 URL로 변경
# [여기에_토큰] 부분을 실제 토큰으로 교체하세요
git remote set-url origin https://[여기에_토큰]@github.com/kkr2652199-prog/RohaTax-App.git
```

```bash
# 3단계: 변경 확인
git remote -v
# 출력에 토큰이 포함된 URL이 보여야 함
```

```bash
# 4단계: 태그 푸시 (이제 인증 없이 가능)
git push origin --tags
```

```bash
# 5단계: 푸시 확인
git ls-remote --tags origin | grep 20260101
# v1.0.0-stable-20260101 태그가 보이면 성공!
```

---

## ✅ 완료 확인

```bash
# 하늘저장소의 태그 목록 확인
git ls-remote --tags origin | grep 20260101
```

**예상 결과:**
```
refs/tags/v1.0.0-stable-20260101
```

---

## ⚠️ 주의사항

1. **토큰 보안**: 토큰은 비밀번호처럼 관리하세요
2. **토큰 만료**: 만료되면 새로 생성해야 합니다
3. **히스토리**: 토큰이 명령어 히스토리에 남을 수 있으니 주의하세요

---

## 🆘 문제 해결

### 문제: "fatal: Authentication failed"
- **원인**: 토큰이 잘못되었거나 만료됨
- **해결**: 새 토큰 생성 후 다시 시도

### 문제: "remote: Invalid username or token"
- **원인**: 토큰 형식이 잘못됨
- **해결**: 토큰 앞에 `ghp_`가 있는지 확인

---

**이 가이드를 따라하시면 태그 푸시가 완료됩니다!**

