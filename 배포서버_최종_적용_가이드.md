# 배포 서버 최종 적용 가이드

**상태**: 로컬에서 빌드 파일 확인 및 Git 푸시 완료  
**다음 단계**: 배포 서버에서만 실행하면 됩니다

---

## ✅ 로컬에서 완료된 작업

- ✅ `index-DofkCTji.js` 파일 확인 완료
- ✅ Git에 추가 및 푸시 완료
- ✅ CSP 설정 수정 완료

---

## 📋 배포 서버에서 실행할 명령어

### 배포 서버 터미널에서 순서대로 실행:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App
```

```bash
# 2. 하늘저장소에서 최신 코드 가져오기
git pull origin main
```

```bash
# 3. JS 파일 확인
ls -lh kweon21/dist/assets/index-DofkCTji.js
```

**확인 사항:**
- 파일이 존재하는가?
- 파일 크기가 0이 아닌가? (약 516KB 정도)

```bash
# 4. 서비스 재시작
sudo systemctl restart rohatax
```

```bash
# 5. 서비스 상태 확인
sudo systemctl status rohatax
```

**확인 사항:**
- `active (running)` 상태인가?

---

## 🌐 브라우저에서 확인

1. **브라우저 캐시 삭제** (매우 중요!)
   - Chrome: `Ctrl + Shift + Delete`
   - 또는 시크릿 모드로 접속

2. **https://www.rohatax.com/studio/ 접속**

3. **확인 사항:**
   - React 앱이 iframe 안에 정상적으로 로드되는가?
   - 콘솔에 오류가 없는가? (F12 → Console)

---

## ✅ 완료 체크리스트

- [x] 로컬에서 빌드 파일 확인 완료
- [x] Git에 추가 및 푸시 완료
- [ ] 배포 서버에서 `git pull` 실행
- [ ] 배포 서버에서 JS 파일 확인
- [ ] 배포 서버에서 서비스 재시작
- [ ] 브라우저에서 정상 작동 확인

---

## 🎯 배포 서버에서 실행할 명령어 (한 번에 복사)

```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
ls -lh kweon21/dist/assets/index-DofkCTji.js
sudo systemctl restart rohatax
sudo systemctl status rohatax
```

---

**배포 서버에서 위 명령어만 실행하시면 됩니다!**

