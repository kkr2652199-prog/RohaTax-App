# 배포서버 구글 API 키 수정 - 단계별 가이드

## 현재 상황
- Git pull 시 로컬 변경사항 충돌 발생
- 진단 스크립트 실행 중 Flask 모듈 에러 발생
- 사용자 ID 2 (tlschs21)는 API 키가 있음
- 환경변수 API 키는 설정되어 있지만 전체 값 확인 필요

---

## 1단계: 로컬 변경사항 처리

배포서버 터미널에서 실행:

```bash
cd /home/ubuntu/RohaTax-App

# 로컬 변경사항 확인
git status

# 로컬 변경사항을 임시 저장 (stash)
git stash

# 또는 변경사항을 버리고 최신 코드로 덮어쓰기 (주의: 로컬 변경사항 손실)
# git checkout -- scripts/deploy_server_google_key_diagnosis.py

# 최신 코드 가져오기
git pull origin main
```

---

## 2단계: 진단 스크립트 재실행

```bash
# 진단 스크립트 실행
python3 scripts/deploy_server_google_key_diagnosis.py
```

**예상 결과:**
- 데이터베이스 사용자별 API 키 확인
- 환경변수 API 키 전체 값 확인 (길이, 앞/뒤 10자)
- 가상환경을 사용한 API 키 조회 로직 테스트
- 최근 로그에서 에러 확인

---

## 3단계: 진단 결과 확인 및 문제 파악

진단 결과에서 확인할 사항:

1. **데이터베이스 API 키:**
   - 사용자 ID 2 (tlschs21): API 키가 있는지
   - API 키 형식이 올바른지 (AIzaSy로 시작)

2. **환경변수 API 키:**
   - GOOGLE_API_KEY 전체 길이
   - GEMINI_API_KEY 전체 길이
   - API 키 형식 검증 결과

3. **API 키 조회 로직 테스트:**
   - _get_api_key 함수가 정상 작동하는지
   - 사용자별 키를 우선 조회하는지
   - 환경변수로 fallback이 작동하는지

4. **로그 에러:**
   - 최근 로그에서 API 키 관련 에러가 있는지

---

## 4단계: 문제 수정 (필요시)

진단 결과를 바탕으로 수정:

### 문제 1: 환경변수 API 키가 잘못된 경우
```bash
# .env 파일 확인
cat .env | grep GOOGLE_API_KEY

# .env 파일 수정 (필요시)
nano .env

# 환경변수 다시 로드 (systemd 서비스 재시작 필요)
sudo systemctl restart rohatax
```

### 문제 2: 사용자별 API 키가 없는 경우
- 마이페이지에서 Google API Key 등록 필요
- 또는 환경변수 API 키 사용

### 문제 3: 코드 수정이 필요한 경우
- 로컬에서 수정 후 git push
- 배포서버에서 git pull
- Flask 서비스 재시작

---

## 5단계: Flask 서비스 재시작 (수정 후)

**중요:** 코드 수정이나 환경변수 변경 후에는 반드시 재시작 필요!

```bash
# Flask 서비스 재시작
sudo systemctl restart rohatax

# 서비스 상태 확인
sudo systemctl status rohatax

# 로그 확인 (에러가 있는지)
sudo journalctl -u rohatax -n 50 --no-pager
```

---

## 6단계: 실제 테스트

브라우저에서 테스트:

1. `https://www.rohatax.com/studio/` 접속
2. 로그인 (tlschs21 / kkr75223996)
3. "카테고리별 주제 추천받기" 클릭
4. 정상 작동하는지 확인

---

## 재시작이 필요한 경우

다음 경우에는 **반드시** Flask 서비스 재시작 필요:

- ✅ 코드 수정 후 (routes/playground_routes/studio_api.py 등)
- ✅ 환경변수 변경 후 (.env 파일 수정)
- ✅ 데이터베이스 스키마 변경 후
- ✅ Python 패키지 설치 후

**재시작 명령어:**
```bash
sudo systemctl restart rohatax
```

---

## 재시작이 필요 없는 경우

다음 경우에는 재시작 불필요:

- ❌ 프론트엔드 파일만 수정 (kweon21/dist 등)
- ❌ 정적 파일만 수정 (static/css, static/js 등)
- ❌ 데이터베이스 데이터만 변경 (사용자 정보 등)

---

## 다음 단계

1. 위 1단계부터 차근차근 진행
2. 각 단계 결과를 확인
3. 문제가 발견되면 해당 문제만 수정
4. 수정 후 재시작
5. 테스트

**진단 결과를 공유해주시면 정확한 수정 방안을 제시하겠습니다!**


