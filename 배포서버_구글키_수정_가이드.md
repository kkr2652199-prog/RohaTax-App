# 배포 서버 Google API 키 수정 가이드

**상태**: 로컬에서 수정 완료  
**다음 단계**: 배포 서버에 적용

---

## ✅ 문제 분석

### 발견된 문제
1. **`studio_api.py`가 서버 환경변수에서만 API 키를 찾음**
   - 사용자별 API 키(BYOK 모델)를 사용하지 않음
   - 환경변수가 없으면 계속 에러 발생 → CPU 상승 원인

2. **로컬 vs 배포 서버 차이**
   - 로컬: 환경변수 설정되어 있거나 프론트엔드에서 직접 호출
   - 배포 서버: 환경변수 없음 → 에러 반복 → CPU 상승

---

## ✅ 수정 내용

### 변경 사항
1. **`_get_api_key()` 함수 수정**
   - 사용자별 API 키를 데이터베이스에서 우선 조회
   - 없으면 서버 환경변수에서 fallback
   - 에러 메시지 개선

2. **에러 처리 개선**
   - 명확한 에러 코드 반환
   - CPU 상승 방지를 위한 적절한 에러 처리

---

## 📋 배포 서버에서 실행할 명령어

### 1단계: 프로젝트 디렉토리로 이동
```bash
cd /home/ubuntu/RohaTax-App
```

### 2단계: 하늘저장소에서 최신 코드 가져오기
```bash
git pull origin main
```

### 3단계: 서비스 재시작
```bash
sudo systemctl restart rohatax
```

### 4단계: 서비스 상태 확인
```bash
sudo systemctl status rohatax
```

**확인 사항:**
- `active (running)` 상태인가?
- 에러 로그가 없는가?

### 5단계: 로그 확인 (선택사항)
```bash
sudo journalctl -u rohatax -n 50 --no-pager
```

**확인 사항:**
- API 키 관련 에러가 사라졌는가?
- 정상적으로 작동하는가?

---

## 🌐 브라우저에서 확인

1. **브라우저 캐시 삭제** (중요!)
   - Chrome: `Ctrl + Shift + Delete`
   - 또는 시크릿 모드로 접속

2. **https://www.rohatax.com/studio/ 접속**

3. **확인 사항:**
   - React 앱이 정상적으로 로드되는가?
   - 콘솔에 API 키 관련 에러가 없는가? (F12 → Console)
   - CPU 사용량이 정상적으로 유지되는가?

---

## ✅ 완료 체크리스트

- [x] 로컬에서 코드 수정 완료
- [x] Git에 푸시 완료
- [ ] 배포 서버에서 `git pull` 실행
- [ ] 배포 서버에서 서비스 재시작
- [ ] 브라우저에서 정상 작동 확인
- [ ] CPU 사용량 정상 확인

---

## 🎯 배포 서버에서 실행할 명령어 (한 번에 복사)

```bash
cd /home/ubuntu/RohaTax-App
git pull origin main
sudo systemctl restart rohatax
sudo systemctl status rohatax
```

---

## 📝 추가 정보

### Google API 키 관리 방식 (BYOK 모델)
- **사용자별 키**: 각 사용자가 마이페이지에서 등록
- **데이터베이스 저장**: `users.google_api_key` 컬럼
- **Fallback**: 서버 환경변수 `GOOGLE_API_KEY` 또는 `GEMINI_API_KEY`

### CPU 상승 원인 해결
- 이전: 환경변수 없음 → 에러 반복 → CPU 상승
- 수정 후: 사용자별 키 우선 사용 → 에러 감소 → CPU 정상화

---

**배포 서버에서 위 명령어만 실행하시면 됩니다!**

