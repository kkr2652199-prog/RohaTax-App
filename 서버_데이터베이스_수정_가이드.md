# 🔧 서버 데이터베이스 수정 가이드

## 📋 발견된 문제
1. ❌ "no such table: orders" - orders 테이블 없음
2. ❌ 데이터베이스 마이그레이션 미실행
3. ❌ 일부 테이블 누락

---

## 1단계: 현재 데이터베이스 상태 확인

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute(\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\"); tables = [row[0] for row in cursor.fetchall()]; print('현재 테이블:', ', '.join(tables)); conn.close()"
```

**확인 사항:**
- `orders` 테이블이 있는지
- `products` 테이블이 있는지
- `activity_logs` 테이블이 있는지

---

## 2단계: 마이그레이션 실행

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "from core.db import _apply_migrations; import sqlite3; conn = sqlite3.connect('database/app.db'); _apply_migrations(conn); conn.commit(); conn.close(); print('✅ 마이그레이션 완료')"
```

---

## 3단계: orders 테이블 생성 (없는 경우)

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 << 'EOF'
import sqlite3

conn = sqlite3.connect('database/app.db')
cursor = conn.cursor()

# orders 테이블 생성
try:
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS orders (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER NOT NULL,
            product_id INTEGER NOT NULL,
            merchant_uid TEXT UNIQUE NOT NULL,
            order_uid TEXT UNIQUE,
            status TEXT DEFAULT 'pending',
            amount INTEGER DEFAULT 0,
            token_amount INTEGER DEFAULT 0,
            supply_price INTEGER DEFAULT 0,
            vat INTEGER DEFAULT 0,
            quantity INTEGER DEFAULT 1,
            payment_method TEXT,
            product_name TEXT,
            tax_evidence_requested INTEGER DEFAULT 0,
            created_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime')),
            updated_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime')),
            FOREIGN KEY(user_id) REFERENCES users(id),
            FOREIGN KEY(product_id) REFERENCES products(id)
        )
    ''')
    
    # 인덱스 생성
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_orders_user_id ON orders(user_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_orders_status ON orders(status)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_orders_merchant_uid ON orders(merchant_uid)')
    
    conn.commit()
    print('✅ orders 테이블 생성 완료')
except Exception as e:
    print(f'⚠️ 오류: {e}')
finally:
    conn.close()
EOF
```

---

## 4단계: products 테이블 확인 및 초기 데이터 삽입

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM products'); count = cursor.fetchone()[0]; print(f'상품 수: {count}'); conn.close()"
```

**상품이 없으면:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 scripts/seed_products_server.py
```

---

## 5단계: 모든 테이블 재확인

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute(\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\"); tables = [row[0] for row in cursor.fetchall()]; print('\\n'.join(tables)); conn.close()"
```

**필수 테이블 확인:**
- ✅ users
- ✅ products
- ✅ orders
- ✅ activity_logs
- ✅ payment_history
- ✅ token_history

---

## 6단계: 서버 재시작 (변경사항 적용)

<--- 복사하세요
```
pkill -f "python3 app.py"
```

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
sudo -E env "PATH=$PATH" "FLASK_RUN_HOST=0.0.0.0" "FLASK_RUN_PORT=80" /home/ubuntu/RohaTax-App/venv/bin/python3 app.py > app.log 2>&1 &
```

<--- 복사하세요
```
sleep 5 && tail -20 app.log
```

---

## 완료 후 확인

브라우저에서:
1. `https://rohatax.com/shop` - 상품 구매 페이지 테스트
2. `https://rohatax.com/admin` - 관리자 대시보드 테스트

오류가 사라졌는지 확인하세요.

