# 배포 서버 CPU 급상승 진단 가이드

**상태**: 진단 스크립트 준비 완료  
**목적**: 배포 서버 접속 시 CPU 급상승 원인 파악

---

## 🔍 진단 방법

### 방법 1: Bash 스크립트 사용 (권장)

배포 서버 터미널에서 실행:

```bash
cd /home/ubuntu/RohaTax-App
bash scripts/deploy_server_cpu_diagnosis.sh
```

### 방법 2: Python 스크립트 사용

배포 서버 터미널에서 실행:

```bash
cd /home/ubuntu/RohaTax-App
python3 scripts/deploy_server_cpu_diagnosis.py
```

---

## 📋 진단 항목

진단 스크립트는 다음을 확인합니다:

1. **시스템 기본 정보**
   - 호스트명, Uptime

2. **CPU 사용량 상위 프로세스**
   - 어떤 프로세스가 CPU를 많이 사용하는지

3. **메모리 사용량**
   - 메모리 부족 여부 확인

4. **디스크 사용량**
   - 디스크 공간 부족 여부 확인

5. **Flask 서비스 상태**
   - systemctl status 확인

6. **최근 서비스 로그 (에러 위주)**
   - 에러, 예외, 실패 로그 확인

7. **Python 프로세스 상세 정보**
   - Python/Gunicorn 프로세스 확인

8. **활성 네트워크 연결 수**
   - DDoS 공격 또는 무한 루프 가능성 확인

9. **데이터베이스 상태**
   - 파일 크기, 잠금 상태 확인

10. **시스템 부하 평균**
    - 1분, 5분, 15분 부하 확인

11. **I/O 대기 프로세스**
    - 디스크 I/O 병목 확인

---

## 🎯 예상되는 문제 원인

### 1. API 키 관련 무한 루프
- **증상**: Python 프로세스가 CPU 100% 사용
- **원인**: API 키가 없어서 계속 재시도
- **해결**: 사용자별 API 키 등록 또는 서버 환경변수 설정

### 2. 데이터베이스 잠금
- **증상**: I/O 대기 프로세스 증가
- **원인**: 데이터베이스 파일이 여러 프로세스에 의해 동시 접근
- **해결**: 데이터베이스 연결 풀 최적화

### 3. 네트워크 연결 과다
- **증상**: ESTABLISHED 연결 수가 비정상적으로 많음
- **원인**: DDoS 공격 또는 무한 루프
- **해결**: 방화벽 설정 또는 코드 수정

### 4. 메모리 부족
- **증상**: 메모리 사용량 90% 이상
- **원인**: 메모리 누수 또는 과도한 요청
- **해결**: 메모리 사용량 최적화

### 4. 에러 로그 패턴
- **증상**: 특정 에러가 반복적으로 발생
- **원인**: 코드 버그 또는 설정 오류
- **해결**: 에러 로그 분석 후 코드 수정

---

## 📊 진단 결과 해석

### 정상 상태
- CPU 사용량: 10% 이하 (유휴 상태)
- 메모리 사용량: 50% 이하
- 네트워크 연결: 10개 이하
- 에러 로그: 없음 또는 최소

### 문제 상태
- CPU 사용량: 50% 이상
- 메모리 사용량: 90% 이상
- 네트워크 연결: 100개 이상
- 에러 로그: 반복적인 에러 패턴

---

## 🔧 즉시 조치 사항

### CPU 사용량이 높은 경우

1. **프로세스 확인**
   ```bash
   top
   # 또는
   htop
   ```

2. **특정 프로세스 종료 (필요시)**
   ```bash
   kill -9 [PID]
   ```

3. **서비스 재시작**
   ```bash
   sudo systemctl restart rohatax
   ```

### 데이터베이스 잠금인 경우

1. **데이터베이스 잠금 확인**
   ```bash
   lsof /home/ubuntu/RohaTax-App/database/app.db
   ```

2. **서비스 재시작**
   ```bash
   sudo systemctl restart rohatax
   ```

### 네트워크 연결 과다인 경우

1. **연결 확인**
   ```bash
   netstat -an | grep ESTABLISHED | wc -l
   ```

2. **의심스러운 IP 확인**
   ```bash
   netstat -an | grep ESTABLISHED | awk '{print $5}' | cut -d: -f1 | sort | uniq -c | sort -rn | head -10
   ```

---

## 📝 진단 결과 공유

진단 결과를 확인한 후:

1. **CPU 사용량이 높은 프로세스** 스크린샷 또는 텍스트
2. **에러 로그** (최근 50줄)
3. **네트워크 연결 수**
4. **데이터베이스 상태**

위 정보를 공유해주시면 정확한 해결책을 제시하겠습니다.

---

## 🚀 다음 단계

1. 진단 스크립트 실행
2. 결과 확인 및 분석
3. 문제 원인 파악
4. 해결책 적용
5. 재진단으로 확인

---

**배포 서버에서 진단 스크립트를 실행하고 결과를 공유해주세요!**




