# 🔍 로컬 vs 인터넷 서버 차이점 확인 가이드

## 📋 목표
로컬에서는 정상 작동하는데 인터넷 서버에서만 문제가 있는 원인 찾기

---

## 1단계: .env 파일 확인 (현재 진행 중)

**Lightsail 터미널에서:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
cat .env
```

**확인 사항:**
- SECRET_KEY가 있는지
- ENVIRONMENT=production인지
- DEBUG=False인지

**로컬과 비교:**
- 로컬의 `.env` 파일 내용과 동일한지 확인

---

## 2단계: 데이터베이스 상태 확인

**서버에서:**

<--- 복사하세요
```
ls -la database/app.db
```

**확인 사항:**
- 파일이 존재하는지
- 권한이 올바른지 (ubuntu 소유여야 함)

**로컬과 비교:**
- 로컬의 `database/app.db`와 서버의 파일 크기 비교

---

## 3단계: 사용자 계정 확인

**서버에서:**

<--- 복사하세요
```
python3 -c "import sqlite3; conn = sqlite3.connect('database/app.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM users'); print('사용자 수:', cursor.fetchone()[0]); conn.close()"
```

**확인 사항:**
- 사용자 계정이 있는지
- 로컬과 동일한 계정이 있는지

---

## 4단계: 정적 파일 확인

**서버에서:**

<--- 복사하세요
```
ls -la static/videos/ | head -10
```

**확인 사항:**
- 로컬에 있는 파일들이 서버에도 있는지
- 특히 `roha_conversion_demo.mp4.mp4` 파일

---

## 5단계: 서버 로그 확인

**서버에서:**

<--- 복사하세요
```
tail -50 app.log | grep -i "error\|warning\|secret"
```

**확인 사항:**
- SECRET_KEY 관련 경고
- 데이터베이스 오류
- 파일 경로 오류

---

## 다음 단계 안내

각 단계를 실행한 후 결과를 알려주시면, 로컬과 서버의 차이점을 찾아 해결하겠습니다.

