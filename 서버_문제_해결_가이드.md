# 서버 app.py 파일 문제 해결 가이드

## 🔍 문제 확인

서버의 `app.py` 파일이 0 바이트(비어있음)입니다.

## ✅ 해결 방법

### 1단계: 프로젝트 디렉토리 찾기
<--- 복사하세요
find ~ -name "app.py" -type f 2>/dev/null | head -5

### 2단계: Git 저장소 위치 확인
<--- 복사하세요
find ~ -name ".git" -type d 2>/dev/null | head -3

### 3단계: 올바른 디렉토리로 이동
(위 명령어 결과를 보고 프로젝트 디렉토리로 이동)

예시:
<--- 복사하세요
cd ~/RohaTax

또는

<--- 복사하세요
cd ~/homepage1

### 4단계: app.py 파일 확인
<--- 복사하세요
ls -lh app.py

<--- 복사하세요
head -20 app.py

### 5단계: 파일이 없거나 비어있다면 Git에서 가져오기
<--- 복사하세요
git checkout HEAD -- app.py

또는

<--- 복사하세요
git pull origin main

---

## 🚀 빠른 해결 (한 번에)

<--- 복사하세요
cd ~ && find . -name "app.py" -type f -size +1k 2>/dev/null | head -1 | xargs dirname

이 명령어로 올바른 프로젝트 디렉토리를 찾은 후:

<--- 복사하세요
cd [위에서 찾은 경로]

<--- 복사하세요
ls -lh app.py

<--- 복사하세요
head -5 app.py

파일이 정상이면 서버를 시작하세요.

