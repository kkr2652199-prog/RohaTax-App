# 백업 방식 완전 설명

**질문**: 백업 파일은 배포 서버에서 Git으로 저장한 것인가요? Git에 모든 것이 저장되나요?

---

## ✅ 우리가 한 작업

### 1. 배포 서버에서 Git 태그 생성
- **위치**: 배포 서버 (AWS Lightsail)
- **작업**: `git tag -a v1.0.0-stable-20260101` 명령어 실행
- **의미**: 현재 코드 상태를 Git 태그로 저장

### 2. 하늘저장소(GitHub)에 푸시
- **작업**: `git push origin --tags` 명령어 실행
- **의미**: Git 태그를 하늘저장소에 업로드

**결론**: 네, 우리가 한 작업입니다!

---

## 📦 Git에 저장되는 것

### ✅ 저장되는 것

1. **모든 코드 파일**
   - Python 파일 (.py)
   - HTML 파일 (.html)
   - CSS 파일 (.css)
   - JavaScript 파일 (.js)
   - 설정 파일 (.json, .toml 등)
   - 스크립트 파일 (.sh, .bat)

2. **프로젝트 구조**
   - 모든 폴더 구조
   - 파일 위치
   - 파일 내용

3. **Git 태그**
   - 특정 시점의 코드 상태
   - 복원할 수 있는 버전 정보

### ❌ 저장되지 않는 것

1. **데이터베이스 파일**
   - `database/app.db` (실제 데이터)
   - **이유**: Git은 코드를 저장하는 도구이지, 데이터베이스를 저장하는 도구가 아닙니다

2. **환경 설정 파일**
   - `.env` 파일 (비밀번호, API 키 등)
   - **이유**: 보안상의 이유로 Git에 저장하지 않습니다

3. **로그 파일**
   - `logs/` 폴더의 로그 파일들
   - **이유**: 계속 변하는 파일이므로 저장하지 않습니다

4. **임시 파일**
   - `__pycache__/` (Python 캐시)
   - `.pyc` 파일들

---

## 🔍 Git 태그 백업의 의미

### Git 태그란?
- **비유**: 책갈피 같은 것
- **의미**: 특정 시점의 코드 상태를 기억하는 표시
- **용도**: 나중에 그 시점으로 돌아갈 수 있음

### 우리가 한 백업
```
v1.0.0-stable-20260101
```
이 태그는 **2026년 1월 1일의 코드 상태**를 기억합니다.

---

## 📊 백업 종류 비교

### 1. Git 태그 백업 (우리가 한 것)
- **저장 위치**: 하늘저장소 (GitHub)
- **저장 내용**: 모든 코드 파일
- **저장 안 됨**: 데이터베이스, .env 파일
- **장점**: 간단하고 빠름
- **용도**: 코드 복원

### 2. 전체 스냅샷 백업 (아직 안 함)
- **저장 위치**: 배포 서버의 백업 폴더
- **저장 내용**: 모든 파일 (코드 + 데이터베이스 + 설정)
- **장점**: 완전한 백업
- **단점**: 용량이 큼
- **용도**: 완전 복원

### 3. 데이터베이스만 백업 (아직 안 함)
- **저장 위치**: 배포 서버의 백업 폴더
- **저장 내용**: 데이터베이스 파일만
- **용도**: 데이터만 복원

---

## 🎯 현재 상황 정리

### 우리가 한 것
1. ✅ 배포 서버의 **코드 상태**를 Git 태그로 저장
2. ✅ 하늘저장소(GitHub)에 푸시 완료
3. ✅ 코드 복원 가능

### 아직 안 한 것
1. ❌ 데이터베이스 백업
2. ❌ 전체 스냅샷 백업
3. ❌ 환경 설정 파일 백업

---

## 💡 결론

### 질문 1: 배포 서버에서 Git으로 저장한 것인가요?
**답**: 네, 맞습니다. 배포 서버에서 Git 태그를 생성하고 하늘저장소에 푸시했습니다.

### 질문 2: 우리가 한 것이 아닌가요?
**답**: 네, 우리가 한 작업입니다. 배포 서버 터미널에서 명령어를 실행했습니다.

### 질문 3: Git에 모든 것이 저장되나요?
**답**: 아니요. Git에는 **코드 파일만** 저장됩니다.
- ✅ 저장됨: 모든 코드 파일
- ❌ 저장 안 됨: 데이터베이스, .env 파일, 로그 파일

---

## 🔄 완전한 백업을 원한다면

### 추가로 해야 할 것
1. **데이터베이스 백업**: `scripts/backup_database.sh` 실행
2. **전체 스냅샷 백업**: `scripts/create_deployment_backup.sh` 실행

하지만 **Git 태그 백업만으로도 코드 복원은 가능**합니다!

---

## ✅ 요약

- **Git 태그 백업**: 코드 상태 저장 (완료 ✅)
- **저장 위치**: 하늘저장소 (GitHub)
- **복원 가능**: 코드는 복원 가능
- **복원 불가**: 데이터베이스는 별도 백업 필요

**현재 상태**: 코드 백업 완료! 데이터베이스 백업은 선택사항입니다.

