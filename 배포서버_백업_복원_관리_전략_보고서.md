# 배포서버 백업 및 복원 관리 전략 보고서

**작성 일시**: 2026-01-01  
**대상**: 배포 서버 (Linux, 터미널 전용)  
**목적**: 현재 안정 상태를 백업하고, 문제 발생 시 복원 가능하도록 관리

---

## 📋 목차

1. [현재 상황 분석](#현재-상황-분석)
2. [백업 전략](#백업-전략)
3. [복원 전략](#복원-전략)
4. [터미널에서 실행 가능한 작업](#터미널에서-실행-가능한-작업)
5. [실행 절차](#실행-절차)

---

## 현재 상황 분석

### 배포 서버 환경
- **OS**: Linux (Ubuntu)
- **접근 방식**: SSH 터미널만 가능 (GUI 없음)
- **서비스 관리**: systemd (rohatax.service)
- **웹 서버**: gunicorn
- **프로젝트 경로**: `/home/ubuntu/RohaTax-App`
- **현재 상태**: 100% 완벽하지 않지만 동작 중

### 백업이 필요한 항목
1. **코드베이스**: Git 저장소 상태
2. **데이터베이스**: `database/app.db`
3. **환경 설정**: `.env` 파일
4. **시스템 설정**: systemd 서비스 파일
5. **로그 파일**: 문제 진단용

---

## 백업 전략

### 1. Git 태그를 이용한 버전 관리 (추천)

**장점:**
- 코드와 함께 버전 정보가 저장됨
- 언제든지 특정 버전으로 복원 가능
- 하늘저장소(GitHub)에 자동 백업됨

**방법:**
```bash
# 배포 서버에서 (터미널)
cd /home/ubuntu/RohaTax-App

# 현재 상태 확인
git status
git log --oneline -5

# 안정 버전으로 태그 생성
git tag -a v1.0.0-stable-$(date +%Y%m%d) -m "현재 안정 버전 백업"

# 하늘저장소에 태그 푸시
git push origin --tags
```

### 2. 전체 프로젝트 스냅샷 백업

**백업 스크립트 생성** (`scripts/create_deployment_backup.sh`):
```bash
#!/bin/bash
# 배포 서버 백업 스크립트

BACKUP_DIR="/home/ubuntu/deployment_backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_NAME="rohatax_backup_${TIMESTAMP}"
BACKUP_PATH="${BACKUP_DIR}/${BACKUP_NAME}"

# 백업 디렉토리 생성
mkdir -p ${BACKUP_DIR}

# 프로젝트 디렉토리 전체 복사
cp -r /home/ubuntu/RohaTax-App ${BACKUP_PATH}

# 데이터베이스 별도 백업
mkdir -p ${BACKUP_PATH}/database_backup
cp /home/ubuntu/RohaTax-App/database/app.db ${BACKUP_PATH}/database_backup/

# 환경 설정 파일 백업
cp /home/ubuntu/RohaTax-App/.env ${BACKUP_PATH}/.env.backup

# systemd 서비스 파일 백업
sudo cp /etc/systemd/system/rohatax.service ${BACKUP_PATH}/rohatax.service.backup

# 압축
cd ${BACKUP_DIR}
tar -czf ${BACKUP_NAME}.tar.gz ${BACKUP_NAME}
rm -rf ${BACKUP_NAME}

echo "백업 완료: ${BACKUP_PATH}.tar.gz"
```

### 3. 데이터베이스 전용 백업

**데이터베이스 백업 스크립트** (`scripts/backup_database.sh`):
```bash
#!/bin/bash
# 데이터베이스 백업 스크립트

BACKUP_DIR="/home/ubuntu/RohaTax-App/database/backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/app_db_${TIMESTAMP}.db"

mkdir -p ${BACKUP_DIR}

# SQLite 데이터베이스 백업
sqlite3 /home/ubuntu/RohaTax-App/database/app.db ".backup ${BACKUP_FILE}"

echo "데이터베이스 백업 완료: ${BACKUP_FILE}"

# 오래된 백업 삭제 (30일 이상)
find ${BACKUP_DIR} -name "app_db_*.db" -mtime +30 -delete
```

---

## 복원 전략

### 1. Git 태그를 이용한 복원

**복원 절차:**
```bash
# 배포 서버에서 (터미널)
cd /home/ubuntu/RohaTax-App

# 서비스 중지
sudo systemctl stop rohatax

# 현재 변경사항 확인
git status

# 특정 태그로 복원
git fetch origin --tags
git checkout tags/v1.0.0-stable-20260101

# 또는 특정 커밋으로 복원
git checkout 9d5a007

# 서비스 재시작
sudo systemctl start rohatax
sudo systemctl status rohatax
```

### 2. 스냅샷 백업으로 복원

**복원 절차:**
```bash
# 배포 서버에서 (터미널)

# 서비스 중지
sudo systemctl stop rohatax

# 백업 파일 확인
ls -lh /home/ubuntu/deployment_backups/

# 백업 압축 해제
cd /home/ubuntu/deployment_backups/
tar -xzf rohatax_backup_20260101_120000.tar.gz

# 프로젝트 디렉토리 백업
mv /home/ubuntu/RohaTax-App /home/ubuntu/RohaTax-App.backup_$(date +%Y%m%d)

# 백업 복원
cp -r rohatax_backup_20260101_120000 /home/ubuntu/RohaTax-App

# 데이터베이스 복원
cp rohatax_backup_20260101_120000/database_backup/app.db /home/ubuntu/RohaTax-App/database/

# 환경 설정 복원
cp rohatax_backup_20260101_120000/.env.backup /home/ubuntu/RohaTax-App/.env

# systemd 서비스 파일 복원 (필요시)
sudo cp rohatax_backup_20260101_120000/rohatax.service.backup /etc/systemd/system/rohatax.service
sudo systemctl daemon-reload

# 서비스 재시작
sudo systemctl start rohatax
sudo systemctl status rohatax
```

### 3. 데이터베이스만 복원

**데이터베이스 복원 절차:**
```bash
# 배포 서버에서 (터미널)

# 서비스 중지
sudo systemctl stop rohatax

# 현재 데이터베이스 백업 (안전장치)
cp /home/ubuntu/RohaTax-App/database/app.db /home/ubuntu/RohaTax-App/database/app.db.before_restore

# 백업 데이터베이스 복원
cp /home/ubuntu/RohaTax-App/database/backups/app_db_20260101_120000.db /home/ubuntu/RohaTax-App/database/app.db

# 권한 설정
chown ubuntu:ubuntu /home/ubuntu/RohaTax-App/database/app.db
chmod 644 /home/ubuntu/RohaTax-App/database/app.db

# 서비스 재시작
sudo systemctl start rohatax
sudo systemctl status rohatax
```

---

## 터미널에서 실행 가능한 작업

### 1. 현재 상태 확인

```bash
# 서비스 상태 확인
sudo systemctl status rohatax

# 프로세스 확인
ps aux | grep gunicorn
ps aux | grep python

# 포트 확인
sudo netstat -tlnp | grep :80
sudo ss -tlnp | grep :80

# 디스크 사용량 확인
df -h
du -sh /home/ubuntu/RohaTax-App

# Git 상태 확인
cd /home/ubuntu/RohaTax-App
git status
git log --oneline -10
```

### 2. 백업 생성

```bash
# Git 태그 백업
cd /home/ubuntu/RohaTax-App
git tag -a v1.0.0-stable-$(date +%Y%m%d) -m "안정 버전 백업"
git push origin --tags

# 전체 스냅샷 백업
bash /home/ubuntu/RohaTax-App/scripts/create_deployment_backup.sh

# 데이터베이스 백업
bash /home/ubuntu/RohaTax-App/scripts/backup_database.sh
```

### 3. 복원 작업

```bash
# Git 태그로 복원
cd /home/ubuntu/RohaTax-App
sudo systemctl stop rohatax
git fetch origin --tags
git checkout tags/v1.0.0-stable-20260101
sudo systemctl start rohatax

# 스냅샷으로 복원
bash /home/ubuntu/RohaTax-App/scripts/restore_from_backup.sh
```

### 4. 서비스 관리

```bash
# 서비스 시작
sudo systemctl start rohatax

# 서비스 중지
sudo systemctl stop rohatax

# 서비스 재시작
sudo systemctl restart rohatax

# 서비스 상태 확인
sudo systemctl status rohatax

# 서비스 로그 확인
sudo journalctl -u rohatax -f
sudo journalctl -u rohatax --since "1 hour ago"
```

---

## 실행 절차

### 초기 설정 (1회만 실행)

**1. 백업 스크립트 생성 및 권한 설정**
```bash
# 배포 서버에서
cd /home/ubuntu/RohaTax-App

# 스크립트 파일 생성 (위의 스크립트 내용 복사)
nano scripts/create_deployment_backup.sh
nano scripts/backup_database.sh
nano scripts/restore_from_backup.sh

# 실행 권한 부여
chmod +x scripts/create_deployment_backup.sh
chmod +x scripts/backup_database.sh
chmod +x scripts/restore_from_backup.sh
```

**2. 백업 디렉토리 생성**
```bash
mkdir -p /home/ubuntu/deployment_backups
mkdir -p /home/ubuntu/RohaTax-App/database/backups
```

### 정기 백업 (주기적으로 실행)

**옵션 1: Git 태그 백업 (추천)**
```bash
# 배포 서버에서
cd /home/ubuntu/RohaTax-App
git tag -a v1.0.0-stable-$(date +%Y%m%d) -m "정기 백업 $(date +%Y-%m-%d)"
git push origin --tags
```

**옵션 2: 전체 스냅샷 백업**
```bash
# 배포 서버에서
bash /home/ubuntu/RohaTax-App/scripts/create_deployment_backup.sh
```

**옵션 3: 데이터베이스만 백업**
```bash
# 배포 서버에서
bash /home/ubuntu/RohaTax-App/scripts/backup_database.sh
```

### 문제 발생 시 복원

**1. 현재 상태 확인**
```bash
sudo systemctl status rohatax
cd /home/ubuntu/RohaTax-App
git log --oneline -10
git tag -l
```

**2. 복원 실행**
```bash
# Git 태그로 복원 (추천)
sudo systemctl stop rohatax
cd /home/ubuntu/RohaTax-App
git fetch origin --tags
git checkout tags/v1.0.0-stable-20260101
sudo systemctl start rohatax
sudo systemctl status rohatax
```

---

## 권장 사항

### 1. 백업 주기
- **Git 태그 백업**: 주요 업데이트 전후
- **전체 스냅샷**: 주 1회 (자동화 가능)
- **데이터베이스 백업**: 일 1회 (자동화 가능)

### 2. 백업 보관 기간
- **Git 태그**: 영구 보관 (GitHub에 저장)
- **스냅샷 백업**: 최근 3개월분 보관
- **데이터베이스 백업**: 최근 30일분 보관

### 3. 자동화 (선택사항)
```bash
# crontab 설정 (일일 데이터베이스 백업)
crontab -e

# 매일 새벽 2시에 데이터베이스 백업
0 2 * * * /home/ubuntu/RohaTax-App/scripts/backup_database.sh

# 매주 일요일 새벽 3시에 전체 스냅샷 백업
0 3 * * 0 /home/ubuntu/RohaTax-App/scripts/create_deployment_backup.sh
```

---

## 주의사항

1. **복원 전 항상 현재 상태 백업**: 복원하기 전에 현재 상태를 먼저 백업하세요.
2. **서비스 중지 후 복원**: 복원 작업은 반드시 서비스를 중지한 후 실행하세요.
3. **복원 후 테스트**: 복원 후 서비스가 정상 동작하는지 확인하세요.
4. **권한 확인**: 파일 복원 후 권한이 올바르게 설정되었는지 확인하세요.

---

## 결론

**배포 서버의 백업 및 복원이 터미널에서 완전히 가능합니다.**

- ✅ Git 태그를 이용한 버전 관리 (추천)
- ✅ 전체 스냅샷 백업
- ✅ 데이터베이스 전용 백업
- ✅ 모든 작업이 터미널에서 실행 가능

**현재 안정 상태를 백업하고, 문제 발생 시 언제든지 복원할 수 있습니다.**

