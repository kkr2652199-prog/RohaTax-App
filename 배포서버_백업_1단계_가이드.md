# 배포서버 백업 관리 - 1단계: 현재 상태 백업

**목표**: 배포 서버의 현재 안정 상태를 Git 태그로 백업하기

---

## 📋 1단계: 배포 서버 접속 및 상태 확인

### 작업 내용
배포 서버에 SSH로 접속하여 현재 상태를 확인합니다.

### 실행 명령어 (배포 서버 터미널에서)

```bash
# 1. 배포 서버에 SSH 접속 (로컬 컴퓨터에서)
# (SSH 접속 방법은 사용자의 설정에 따라 다릅니다)
# 예: ssh ubuntu@your-server-ip

# 2. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 3. 현재 Git 상태 확인
git status

# 4. 최근 커밋 확인
git log --oneline -5

# 5. 서비스 상태 확인
sudo systemctl status rohatax
```

### 확인 사항
- ✅ Git 저장소가 정상인지
- ✅ 서비스가 실행 중인지
- ✅ 최근 커밋이 무엇인지

---

## 📋 2단계: 최신 코드 가져오기 (선택사항)

### 작업 내용
하늘저장소에서 최신 백업 스크립트를 가져옵니다.

### 실행 명령어 (배포 서버 터미널에서)

```bash
# 1. 하늘저장소에서 최신 코드 가져오기
cd /home/ubuntu/RohaTax-App
git pull origin main

# 2. 백업 스크립트 파일 확인
ls -la scripts/backup_*.sh
ls -la scripts/create_deployment_backup.sh
ls -la scripts/restore_from_backup.sh
```

### 확인 사항
- ✅ 스크립트 파일들이 있는지
- ✅ 파일 권한이 올바른지

---

## 📋 3단계: 현재 안정 상태 Git 태그 백업

### 작업 내용
현재 안정 상태를 Git 태그로 백업합니다. (가장 중요!)

### 실행 명령어 (배포 서버 터미널에서)

```bash
# 1. 프로젝트 디렉토리로 이동
cd /home/ubuntu/RohaTax-App

# 2. 현재 날짜로 태그 생성
# 오늘 날짜: 2026년 1월 1일
git tag -a v1.0.0-stable-20260101 -m "현재 안정 버전 백업 2026-01-01"

# 3. 태그 확인
git tag -l

# 4. 하늘저장소에 태그 푸시
git push origin --tags
```

### 확인 사항
- ✅ 태그가 생성되었는지
- ✅ 하늘저장소에 태그가 푸시되었는지

---

## ✅ 1단계 완료 확인

### 확인 명령어

```bash
# 태그 목록 확인
git tag -l | tail -5

# 하늘저장소 태그 확인
git ls-remote --tags origin | tail -5
```

### 완료 조건
- ✅ Git 태그가 생성됨
- ✅ 태그가 하늘저장소에 푸시됨
- ✅ 태그 이름에 날짜가 포함됨 (예: v1.0.0-stable-20260101)

---

## 📝 다음 단계 안내

**1단계가 완료되면 다음을 진행하세요:**
- 2단계: 전체 스냅샷 백업 (선택사항)
- 3단계: 데이터베이스 백업 설정 (선택사항)

**하지만 1단계(Git 태그 백업)만으로도 복원이 가능합니다!**

---

## ⚠️ 주의사항

1. **태그 이름 규칙**: `v1.0.0-stable-YYYYMMDD` 형식을 사용하세요
2. **백업 시점**: 서비스가 정상 동작할 때 백업하세요
3. **태그 푸시**: 반드시 `git push origin --tags`로 하늘저장소에 푸시하세요

---

## 🆘 문제 해결

### 문제: Git 태그 생성 실패
```bash
# 해결: Git 상태 확인
git status
# 변경사항이 있으면 커밋하거나 stash
git stash
```

### 문제: 태그 푸시 실패
```bash
# 해결: 원격 저장소 확인
git remote -v
# 인증 문제인지 확인
```

---

**1단계를 완료하셨으면 알려주세요. 다음 단계를 안내하겠습니다.**

