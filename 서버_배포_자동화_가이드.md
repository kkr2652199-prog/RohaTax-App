# 🚀 서버 배포 자동화 가이드

## 📋 개요

로컬에서 개발한 애플리케이션을 인터넷 서버에 배포할 때 필요한 모든 설정을 자동으로 수행하는 스크립트입니다.

---

## 🎯 해결하는 문제들

1. ✅ 데이터베이스 마이그레이션 자동 실행
2. ✅ 누락된 테이블 자동 생성 (orders 등)
3. ✅ 초기 데이터 자동 삽입
4. ✅ 환경 변수 자동 설정
5. ✅ 서버 자동 재시작

---

## 📁 생성된 파일

1. **`scripts/deploy_server.py`**: Python 배포 스크립트
2. **`scripts/server_setup.sh`**: 전체 배포 자동화 스크립트

---

## 🚀 사용 방법

### 방법 1: 전체 자동화 스크립트 (권장)

**Lightsail 터미널에서:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
chmod +x scripts/server_setup.sh
bash scripts/server_setup.sh
```

이 스크립트는 다음을 자동으로 수행합니다:
1. 가상환경 활성화
2. 데이터베이스 권한 수정
3. 데이터베이스 배포 (마이그레이션, 테이블 생성, 초기 데이터)
4. .env 파일 확인 및 생성
5. 서버 재시작

---

### 방법 2: Python 스크립트만 실행

**데이터베이스 배포만 필요한 경우:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
source venv/bin/activate
python3 scripts/deploy_server.py
```

---

## 🔄 업데이트 후 배포

**코드를 업데이트한 후:**

<--- 복사하세요
```
cd /home/ubuntu/RohaTax-App
git pull origin main
bash scripts/server_setup.sh
```

---

## 📝 체크리스트

배포 전 확인:
- [ ] Git에서 최신 코드 가져오기 (`git pull`)
- [ ] 배포 스크립트 실행 (`bash scripts/server_setup.sh`)
- [ ] 서버 로그 확인 (`tail -20 app.log`)
- [ ] 브라우저에서 접속 테스트

---

## 🆘 문제 해결

### 서버가 시작되지 않으면

<--- 복사하세요
```
tail -50 app.log | grep -i "error"
```

### 데이터베이스 오류가 있으면

<--- 복사하세요
```
python3 scripts/deploy_server.py
```

### 포트 충돌이 있으면

<--- 복사하세요
```
lsof -ti:80 | xargs kill -9 2>/dev/null || true
bash scripts/server_setup.sh
```

---

## ✅ 완료 후 확인

1. **브라우저에서 `https://rohatax.com` 접속**
2. **로그인 테스트**
3. **주요 기능 테스트:**
   - 상품 구매 (`/shop`)
   - 변환 기능 (`/conversion`)
   - 관리자 대시보드 (`/admin`)

---

## 🎯 다음 단계

배포 자동화가 완료되었으므로:
1. 본진으로 병합
2. 하늘저장소로 푸시
3. 서버에서 `git pull` 후 `bash scripts/server_setup.sh` 실행

이제 배포가 훨씬 쉬워집니다!

